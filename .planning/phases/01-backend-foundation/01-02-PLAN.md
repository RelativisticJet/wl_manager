---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [bin/wl_constants.py, bin/wl_handler.py, tests/unit/test_constants.py]
autonomous: true
requirements: [BMOD-02]
must_haves:
  truths:
    - "All magic numbers, regex patterns, and role definitions are in wl_constants.py, not hardcoded in handler"
    - "wl_handler.py imports from wl_constants instead of defining them locally"
    - "App functionality is unchanged after extraction (CSV load/save still works)"
    - "Unit tests verify all constants are defined with expected values"
  artifacts:
    - path: "bin/wl_constants.py"
      provides: "All constants, regex patterns, role sets, and path helpers (Layer 0)"
      exports: ["APP_NAME", "MAX_ROWS", "MAX_COLUMNS", "EDIT_ROLES", "ADMIN_ROLES", "_CONTROL_CHAR_RE", "get_splunk_home", "get_detection_rules_path", "get_approval_queue_path"]
      min_lines: 150
    - path: "bin/wl_handler.py"
      provides: "REST handler with imported constants"
      contains: "from wl_constants import"
      min_changes: "Lines 57-190 replaced with import statement"
    - path: "tests/unit/test_constants.py"
      provides: "Unit tests verifying all constants"
      exports: ["test_constants_defined", "test_role_sets_populated", "test_regex_patterns_compile", "test_path_helpers"]
      min_lines: 80
  key_links:
    - from: "bin/wl_handler.py"
      to: "bin/wl_constants.py"
      via: "from wl_constants import"
      pattern: "from wl_constants import"
    - from: "tests/unit/test_constants.py"
      to: "bin/wl_constants.py"
      via: "import wl_constants"
      pattern: "import wl_constants"
---

<objective>
Extract all constants, regex patterns, role definitions, and path helpers from wl_handler.py into a new wl_constants.py module. This establishes Layer 0 (the foundation) for all subsequent Phase 1 module extractions.

Purpose: Create a single source of truth for configuration, eliminating magic numbers and hardcoded values throughout the codebase. All other Phase 1 modules (validation, rbac, logging, ratelimit, presence) will import from this layer.

Output:
- bin/wl_constants.py: ~200 lines, no imports from wl_* modules, contains all constants, regex patterns, role sets, and path helpers
- Updated bin/wl_handler.py: imports from wl_constants instead of defining constants locally
- tests/unit/test_constants.py: ≥80% coverage verifying all constants and helpers
</objective>

<execution_context>
@c:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@c:/Users/PC/wl_manager/.planning/PROJECT.md
@c:/Users/PC/wl_manager/.planning/REQUIREMENTS.md
@c:/Users/PC/wl_manager/.planning/phases/01-backend-foundation/01-CONTEXT.md
@c:/Users/PC/wl_manager/.planning/phases/01-backend-foundation/01-RESEARCH.md
@c:/Users/PC/wl_manager/CLAUDE.md
@c:/Users/PC/wl_manager/bin/wl_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/wl_constants.py with all extracted constants and helpers</name>
  <files>bin/wl_constants.py</files>
  <read_first>
    - bin/wl_handler.py (lines 57-190) — the source code to extract
    - Understand the structure: APP_NAME, SPLUNK_HOME, APPS_DIR, OWN_LOOKUPS, MAPPING_FILE, etc.
    - Note: Constants include phase 2/3 domain constants (APPROVAL_QUEUE_FILE, TRASH_DIR, DEFAULT_LIMITS) per 01-CONTEXT.md locked decision
  </read_first>
  <action>
Create `/c/Users/PC/wl_manager/bin/wl_constants.py` as a new file with Layer 0 constants. This module has NO imports from wl_* modules (no dependencies).

Content should include:

**Basic setup:**
- Docstring explaining this is Layer 0 (no wl_* dependencies)
- `__all__` list explicitly declaring public API

**Imports:**
- Only stdlib: os, re, json (for future config file support)

**Functions:**
- `get_splunk_home() -> str:` — Returns SPLUNK_HOME from environment, defaults to "/opt/splunk", patchable in tests
- `get_detection_rules_path() -> str:` — Returns path to _detection_rules.json
- `get_approval_queue_path() -> str:` — Returns path to _approval_queue.json

**Eager derived constants:**
- Computed from get_splunk_home() at module import time:
  - SPLUNK_HOME = get_splunk_home()
  - APPS_DIR = os.path.join(SPLUNK_HOME, "etc", "apps")
  - OWN_LOOKUPS = os.path.join(APPS_DIR, "wl_manager", "lookups")
  - MAPPING_FILE = os.path.join(OWN_LOOKUPS, "rule_csv_map.csv")
  - Similar for APPROVAL_QUEUE_FILE, TRASH_DIR, VERSION_MANIFEST_FILE

**App metadata:**
- APP_NAME = "wl_manager"

**Limits (from wl_handler.py lines 67-72):**
- MAX_ROWS = 5000
- MAX_COLUMNS = 100
- MAX_CELL_CHARS = 1000
- MAX_PAYLOAD_BYTES = 10 * 1024 * 1024
- MAX_AUDIT_VALUE_LINES = 500
- MAX_DIFF_ROWS = 2000
- MAX_PRESENCE_USERS = 10
- MAX_PRESENCE_FILES = 200

**Timeouts:**
- PRESENCE_TIMEOUT = 60
- IDLE_TIMEOUT = 1800

**Rate limiting (from lines 78-82):**
- RATE_WINDOW = 60
- RATE_MAX_WRITES = 30
- RATE_MAX_READS = 120

**Column names:**
- EXPIRE_COLUMN_NAMES = {"expires", "expire", "expiration", "expiration_date", "expiry", "termination", "termination_date"}

**Role sets (from lines 91-98, including superadmin from 100):**
- EDIT_ROLES = {"wl_editor", "wl_analyst_editor", "wl_admin", "wl_superadmin", "admin", "sc_admin"}
- ADMIN_ROLES = {"admin", "sc_admin", "wl_admin", "wl_superadmin"}
- SUPERADMIN_ROLES = {"wl_superadmin"}

**Audit constants:**
- AUDIT_INDEX = "wl_audit"
- AUDIT_SOURCE = "wl_manager"
- AUDIT_SOURCETYPE = "wl_audit"

**Version control:**
- VERSIONS_DIR = "_versions"
- MAX_VERSIONS = 6

**Regex patterns (compiled, eager):**
- _CONTROL_CHAR_RE = re.compile(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]")
- _SAFE_COLNAME_RE = re.compile(r"^(?=.*[a-zA-Z0-9])[a-zA-Z0-9_\-\.()/:#@&+]+$")
- _SANITIZE_RE = re.compile(r'[^\w\s.,;:!?\'"()\-/@#&+=\[\]{}%$\n\r]', re.UNICODE)

Include comprehensive docstrings for each constant explaining its purpose and usage. Add type hints where appropriate (e.g., EDIT_ROLES: Set[str]).
  </action>
  <verify>
    <automated>grep -q "^APP_NAME = " /c/Users/PC/wl_manager/bin/wl_constants.py && grep -q "^MAX_ROWS = " /c/Users/PC/wl_manager/bin/wl_constants.py && grep -q "^EDIT_ROLES = " /c/Users/PC/wl_manager/bin/wl_constants.py && grep -q "^_CONTROL_CHAR_RE = " /c/Users/PC/wl_manager/bin/wl_constants.py && grep -q "def get_splunk_home" /c/Users/PC/wl_manager/bin/wl_constants.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists at /c/Users/PC/wl_manager/bin/wl_constants.py
    - Contains __all__ list with public API
    - Contains all constants from wl_handler.py lines 57-100: APP_NAME, MAX_ROWS, EDIT_ROLES, AUDIT_INDEX, etc.
    - Contains function get_splunk_home() that returns SPLUNK_HOME env var with default "/opt/splunk"
    - Contains function get_detection_rules_path() returning path to _detection_rules.json
    - Contains function get_approval_queue_path() returning path to _approval_queue.json
    - Contains all 3 role sets: EDIT_ROLES, ADMIN_ROLES, SUPERADMIN_ROLES
    - Contains all 3 regex patterns compiled at module level: _CONTROL_CHAR_RE, _SAFE_COLNAME_RE, _SANITIZE_RE
    - Contains AUDIT_INDEX, AUDIT_SOURCE, AUDIT_SOURCETYPE, VERSIONS_DIR, MAX_VERSIONS
    - Contains RATE_WINDOW, RATE_MAX_WRITES, RATE_MAX_READS
    - Contains PRESENCE_TIMEOUT, IDLE_TIMEOUT, EXPIRE_COLUMN_NAMES
    - Has docstring explaining Layer 0 (no wl_* imports)
    - No imports from wl_* modules (only stdlib)
    - All constants have type hints and docstrings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update bin/wl_handler.py to import from wl_constants</name>
  <files>bin/wl_handler.py</files>
  <read_first>
    - bin/wl_handler.py current state (especially lines 57-190, 268-294)
    - Identify all references to the constants being extracted (search for: MAX_ROWS, EDIT_ROLES, _CONTROL_CHAR_RE, AUDIT_INDEX, etc.)
  </read_first>
  <action>
Update `/c/Users/PC/wl_manager/bin/wl_handler.py` to:

1. **Add import statement at top of file** (after existing imports, around line 30-40 area):
   - Add `import sys`
   - Add `sys.path.insert(0, os.path.dirname(__file__))` to enable same-directory imports
   - Add `from wl_constants import` statement with all constants needed

   Example (extract actual list from wl_constants.py __all__):
   ```python
   sys.path.insert(0, os.path.dirname(__file__))
   from wl_constants import (
       APP_NAME, SPLUNK_HOME, APPS_DIR, OWN_LOOKUPS, MAPPING_FILE,
       MAX_ROWS, MAX_COLUMNS, MAX_CELL_CHARS, MAX_PAYLOAD_BYTES,
       MAX_AUDIT_VALUE_LINES, MAX_DIFF_ROWS, MAX_PRESENCE_USERS, MAX_PRESENCE_FILES,
       PRESENCE_TIMEOUT, IDLE_TIMEOUT, RATE_WINDOW, RATE_MAX_WRITES, RATE_MAX_READS,
       EDIT_ROLES, ADMIN_ROLES, SUPERADMIN_ROLES, EXPIRE_COLUMN_NAMES,
       AUDIT_INDEX, AUDIT_SOURCE, AUDIT_SOURCETYPE, VERSIONS_DIR, MAX_VERSIONS,
       _CONTROL_CHAR_RE, _SANITIZE_RE, _SAFE_COLNAME_RE,
       get_splunk_home, get_detection_rules_path, get_approval_queue_path,
   )
   ```

2. **Delete lines 57-190** from wl_handler.py (the constants section that was extracted)
   - This includes: APP_NAME definition, SPLUNK_HOME, APPS_DIR, OWN_LOOKUPS, all constants, regex definitions
   - Leave other code (imports, classes, methods) intact

3. **Verify no logic changed:**
   - All references to MAX_ROWS, EDIT_ROLES, etc. still work (now from import)
   - No functional changes to the handler, just moved constants

4. **Update MCP deploy tool** (if it exists) to include wl_constants.py in the file list (this will be done in Phase 4, note for now)
  </action>
  <verify>
    <automated>grep -q "from wl_constants import" /c/Users/PC/wl_manager/bin/wl_handler.py && grep -q "sys.path.insert" /c/Users/PC/wl_manager/bin/wl_handler.py && ! grep -q "^SPLUNK_HOME = os.environ" /c/Users/PC/wl_manager/bin/wl_handler.py && grep -q "^AUDIT_INDEX" /c/Users/PC/wl_manager/bin/wl_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - bin/wl_handler.py contains `sys.path.insert(0, os.path.dirname(__file__))`
    - bin/wl_handler.py contains `from wl_constants import` statement
    - Lines 57-190 (original constants section) are deleted from wl_handler.py
    - wl_handler.py no longer defines SPLUNK_HOME = os.environ.get(...) directly
    - wl_handler.py still defines AUDIT_INDEX (from import, but still present in namespace)
    - All remaining code in wl_handler.py is unchanged (no logic modifications)
    - File is syntactically valid (no broken references)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create tests/unit/test_constants.py with unit tests</name>
  <files>tests/unit/test_constants.py</files>
  <read_first>
    - bin/wl_constants.py (newly created) — understand all constants being tested
    - Test structure from 01-RESEARCH.md: pytest with @pytest.mark.unit decorator
  </read_first>
  <action>
Create `/c/Users/PC/wl_manager/tests/unit/test_constants.py` with comprehensive unit tests for wl_constants module. Coverage target: ≥80%.

**Test classes/functions should cover:**

1. **test_all_constants_defined** — Verify all expected constants exist
   - Check APP_NAME, SPLUNK_HOME, MAX_ROWS, MAX_COLUMNS, etc. are defined
   - Verify they are not None and have expected types

2. **test_role_sets_populated** — Verify role set definitions
   - EDIT_ROLES contains expected roles (wl_editor, admin, etc.)
   - ADMIN_ROLES contains expected roles (admin, sc_admin, etc.)
   - SUPERADMIN_ROLES is non-empty
   - Roles are sets (not lists)

3. **test_audit_constants** — Verify audit configuration
   - AUDIT_INDEX == "wl_audit"
   - AUDIT_SOURCE == "wl_manager"
   - AUDIT_SOURCETYPE == "wl_audit"

4. **test_limits_are_integers** — Verify all limit constants are positive integers
   - MAX_ROWS, MAX_COLUMNS, MAX_CELL_CHARS, etc.

5. **test_timeouts_are_integers** — Verify timeout values
   - PRESENCE_TIMEOUT, IDLE_TIMEOUT

6. **test_rate_limiting_values** — Verify rate limit configuration
   - RATE_WINDOW, RATE_MAX_WRITES, RATE_MAX_READS are positive integers

7. **test_regex_patterns_compile** — Verify regex patterns compile without errors
   - _CONTROL_CHAR_RE, _SAFE_COLNAME_RE, _SANITIZE_RE are all compiled patterns
   - Test that they can be used with .search() / .match()

8. **test_get_splunk_home_default** — Verify get_splunk_home() function
   - Returns default "/opt/splunk" when SPLUNK_HOME not set
   - Can be patched in tests with monkeypatch.setenv

9. **test_get_detection_rules_path** — Verify path helper functions
   - get_detection_rules_path() returns a string
   - Contains "_detection_rules.json"
   - Is under OWN_LOOKUPS

10. **test_get_approval_queue_path** — Verify path helper
    - Returns string containing "_approval_queue.json"

11. **test_paths_are_absolute** — Verify derived paths are absolute (not relative)
    - APPS_DIR, OWN_LOOKUPS, MAPPING_FILE all start with /

12. **test_expire_column_names_is_set** — Verify EXPIRE_COLUMN_NAMES
    - Is a set (not list)
    - Contains expected column names (expires, expiration, etc.)

Use @pytest.mark.unit decorator for all tests. Use fixtures from tests/unit/conftest.py (mock_splunk_home, etc.) where appropriate.
  </action>
  <verify>
    <automated>grep -q "def test_" /c/Users/PC/wl_manager/tests/unit/test_constants.py && grep -q "@pytest.mark.unit" /c/Users/PC/wl_manager/tests/unit/test_constants.py && grep -q "import wl_constants" /c/Users/PC/wl_manager/tests/unit/test_constants.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists at /c/Users/PC/wl_manager/tests/unit/test_constants.py
    - Contains import: `import sys; sys.path.insert(0, ...); import wl_constants`
    - Contains at least 10 test functions (test_all_constants_defined, test_role_sets_populated, test_regex_patterns_compile, etc.)
    - All test functions have @pytest.mark.unit decorator
    - Tests verify: constants exist, types are correct, regex patterns compile, role sets are populated
    - Tests use fixtures (mock_splunk_home) where appropriate
    - Can run with: `pytest tests/unit/test_constants.py -v`
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Run unit tests:**
   ```bash
   cd /c/Users/PC/wl_manager
   pytest tests/unit/test_constants.py -v
   ```
   Should pass all test functions.

2. **Check import works:**
   ```bash
   cd /c/Users/PC/wl_manager
   python3 -c "import sys; sys.path.insert(0, 'bin'); from wl_constants import MAX_ROWS, EDIT_ROLES; print('OK')"
   ```

3. **Verify wl_handler.py still works:**
   - No Python syntax errors: `python3 -m py_compile bin/wl_handler.py`
   - All imports resolve: `python3 -c "import sys; sys.path.insert(0, 'bin'); import wl_handler"`

4. **Check coverage:**
   ```bash
   pytest tests/unit/test_constants.py --cov=bin.wl_constants --cov-report=term-missing
   ```
   Should show ≥80% coverage.
</verification>

<success_criteria>
- bin/wl_constants.py created with ~200 lines of constants, no wl_* imports
- bin/wl_handler.py imports from wl_constants (lines 57-190 removed)
- tests/unit/test_constants.py created with ≥10 test functions
- All unit tests pass: `pytest tests/unit/test_constants.py -v`
- Coverage ≥80% for wl_constants module
- wl_handler.py has no syntax errors and imports resolve
- Requirement BMOD-02 addressed: "wl_constants.py extracts all magic numbers, regex patterns, role lists, and config defaults"
</success_criteria>

<output>
After completion, append to `.planning/phases/01-backend-foundation/01-02-SUMMARY.md`:
- Constants layer extracted (wl_constants.py created)
- Unit tests created and passing
- wl_handler.py updated to import from constants
- Coverage: ≥80% for wl_constants
- Ready for Phase 01-03 (wl_logging, wl_validation extractions)
</output>
