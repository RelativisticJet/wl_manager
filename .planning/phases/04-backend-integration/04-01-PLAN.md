---
phase: 04-backend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/wl_replay.py
  - bin/wl_handler.py
  - tests/unit/test_replay.py
  - tests/integration/test_handler_dispatch.py
autonomous: true
requirements: [BMOD-01, TEST-01, TEST-02]
must_haves:
  truths:
    - "GET action handlers execute and return results without functional change"
    - "Handler uses class-level dispatch tables (GET_ACTIONS, POST_ACTIONS)"
    - "All GET actions resolve via shared _dispatch() method"
    - "Dispatch tables contain all GET actions with required roles"
    - "Unknown actions return 400 error"
    - "wl_replay module exists and handles approval action execution"
    - "Dispatch completeness: all _action_* methods are in dispatch tables"
  artifacts:
    - path: "bin/wl_replay.py"
      provides: "Approval action replay orchestration (Layer 5)"
      min_lines: 150
    - path: "bin/wl_handler.py"
      provides: "Thin REST router with dispatch tables and GET handlers"
      exports: ["class WhitelistHandler", "GET_ACTIONS", "POST_ACTIONS", "_dispatch", "_action_*"]
    - path: "tests/integration/test_handler_dispatch.py"
      provides: "Dispatch table structure and completeness tests"
      contains: "test_dispatch_tables_complete"
  key_links:
    - from: "bin/wl_handler.py"
      to: "bin/wl_replay.py"
      via: "handler._action_process_approval delegates to wl_replay.execute_approved_action"
      pattern: "import.*wl_replay"
    - from: "bin/wl_replay.py"
      to: "bin/wl_csv.py, bin/wl_versions.py, bin/wl_rules.py, bin/wl_trash.py"
      via: "replay imports pipeline functions"
      pattern: "from wl_.*import.*pipeline"
    - from: "_dispatch()"
      to: "handler methods"
      via: "getattr(self, method_name) resolves _action_* methods"
      pattern: "getattr.*_action_"
---

<objective>
Implement wl_replay.py (Layer 5 approval orchestration module) and refactor wl_handler.py Wave 1 (GET actions) to use class-level dispatch tables with shared _dispatch() routing.

Purpose:
- Establish replay infrastructure for approval execution
- Migrate all GET handlers (10+ read-only actions) from monolithic inline code to thin dispatch-table-based handlers
- Verify dispatch table pattern works correctly before proceeding to POST handlers

Output:
- wl_replay.py: 150-200 lines, executes approved actions via domain module pipelines
- wl_handler.py: GET_ACTIONS dispatch table, 10+ GET handlers as _action_* methods, shared _dispatch()
- Dispatch completeness tests verifying table consistency
- All GET actions working with zero functional change
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-backend-integration/04-CONTEXT.md
@.planning/phases/04-backend-integration/04-RESEARCH.md

# Phase 1-3 modules for import/pipeline references
@bin/wl_constants.py
@bin/wl_csv.py
@bin/wl_versions.py
@bin/wl_rules.py
@bin/wl_trash.py
@bin/wl_audit.py
@bin/wl_approval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wl_replay.py module (Layer 5 approval orchestration)</name>
  <files>bin/wl_replay.py</files>
  <read_first>
    - bin/wl_handler.py (see current approval replay logic to extract into wl_replay)
    - bin/wl_approval.py (understand queue schema and what replay receives)
    - bin/wl_csv.py (verify save_csv_pipeline signature)
    - bin/wl_versions.py (verify revert_csv_pipeline signature)
    - bin/wl_rules.py (verify create_rule_pipeline signature)
    - bin/wl_trash.py (verify remove_csv_pipeline, remove_rule_pipeline signatures)
    - bin/wl_audit.py (verify build_audit_event, post_audit_event signatures)
  </read_first>
  <action>
Create bin/wl_replay.py (Layer 5): dedicated module for executing approved actions via domain module pipelines.

**Module structure (150-200 lines):**

Imports:
- sys.path.insert(0, os.path.dirname(__file__))
- from wl_csv import save_csv_pipeline, create_csv_pipeline
- from wl_versions import revert_csv_pipeline
- from wl_rules import create_rule_pipeline
- from wl_trash import remove_csv_pipeline, remove_rule_pipeline
- from wl_audit import build_audit_event, post_audit_event
- from wl_logging import get_audit_logger
- Standard: os, json, copy

**Exports:**
```python
__all__ = ["execute_approved_action"]
```

**Functions:**

1. `execute_approved_action(context, request_item)` (60-80 lines):
   - Signature: context={original_analyst, approving_admin, second_admin, is_dual_admin, request_id, action_type, session_key, approved_at}, request_item=dict
   - Validate preconditions: CSV exists (for save/revert), rule exists (for create/delete)
   - Dispatch to action-specific handler via REPLAY_HANDLERS dict
   - Wrap handler call in try/except, return {success, message, data, error, error_type}
   - Log failures to audit logger (non-blocking)
   - Return structured result dict on success/error

2. `_execute_replay_save_csv(context, request_item)` (30-40 lines):
   - Call save_csv_pipeline(csv_file, rows, expected_mtime=None)
   - Post audit event with action="replay_save_csv" via post_audit_event
   - Return {success, message} on success or {success, error, error_type} on failure
   - Exception handling: catch Exception, return error dict, don't re-raise

3. `_execute_replay_revert_csv(context, request_item)` (20-30 lines):
   - Extract version_id from request_item payload
   - Call revert_csv_pipeline(csv_file, version_id, comment)
   - Post audit event with action="replay_revert_csv"
   - Return success dict

4. `_execute_replay_create_rule(context, request_item)` (20-25 lines):
   - Call create_rule_pipeline(rule_name, csv_file)
   - Post audit event
   - Return success dict

5. `_execute_replay_delete_rule(context, request_item)` (20-25 lines):
   - Call remove_rule_pipeline(rule_name, comment)
   - Post audit event
   - Return success dict

6. `_execute_replay_delete_csv(context, request_item)` (20-25 lines):
   - Call remove_csv_pipeline(csv_file, reason, is_bulk)
   - Post audit event
   - Return success dict

7. `_execute_replay_create_csv(context, request_item)` (20-25 lines):
   - Call create_csv_pipeline(csv_name, columns)
   - Post audit event
   - Return success dict

**Dict dispatch inside module:**
```python
REPLAY_HANDLERS = {
    "save_csv": _execute_replay_save_csv,
    "add_row": _execute_replay_save_csv,  # Same pipeline
    "remove_rows": _execute_replay_save_csv,  # Same pipeline
    "create_csv": _execute_replay_create_csv,
    "create_rule": _execute_replay_create_rule,
    "delete_csv": _execute_replay_delete_csv,
    "delete_rule": _execute_replay_delete_rule,
    "revert_csv": _execute_replay_revert_csv,
}
```

**Error handling pattern:**
- Precondition checks in execute_approved_action (CSV exists, rule exists) → return {success: False, error_type: "missing_csv"}
- Handler errors caught with try/except → return {success: False, error, error_type}
- Audit posting failures logged but don't fail the overall operation
- Return dict structure: {success: bool, message: str, data: dict (optional), error: str (optional), error_type: str (optional)}

**Type hints on all public functions.** No custom exception classes — use standard Python exceptions.

**Logger:** Call get_audit_logger() once at module load, use for all failures.
  </action>
  <verify>
    <automated>grep -n "def execute_approved_action" bin/wl_replay.py && grep -n "REPLAY_HANDLERS" bin/wl_replay.py && grep -n "def _execute_replay_" bin/wl_replay.py | wc -l && python -m py_compile bin/wl_replay.py</automated>
  </verify>
  <done>
wl_replay.py exists with 150+ lines; contains execute_approved_action, REPLAY_HANDLERS dict, and 6 action-specific handlers (_execute_replay_*); all functions have type hints; module compiles without syntax errors; imports all required modules from wl_csv, wl_versions, wl_rules, wl_trash, wl_audit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor wl_handler.py Wave 1 — implement dispatch tables and GET handlers</name>
  <files>bin/wl_handler.py</files>
  <read_first>
    - bin/wl_handler.py (current monolithic handler, lines 1-5909, identify all GET actions)
    - bin/wl_constants.py (role definitions: ADMIN_ROLES, EDIT_ROLES, VIEWER_ROLES)
    - 04-CONTEXT.md section "Router Dispatch Pattern" (locked dispatch table design)
  </read_first>
  <action>
Refactor wl_handler.py Wave 1: implement dispatch tables and migrate all GET handlers (10+ read-only actions).

**File organization:**
```
1. Imports (sys, os, json, logging, Splunk SDK, all wl_* modules including wl_replay)
2. Logger setup (get_audit_logger at module level)
3. Dispatch tables (GET_ACTIONS, POST_ACTIONS as class-level constants)
4. WhitelistHandler class:
   a. __init__, handle(), _dispatch()
   b. GET handlers (_action_get_rules, _action_get_csvs, _action_get_csv_content, etc.)
   c. POST handlers (wave 2-3, stubbed or left as comments for now)
   d. Helper methods (_resp, _parse_query, _make_access_log, etc.)
```

**Dispatch tables (class-level constants):**

GET_ACTIONS dict format: `"action_name": (required_roles_set, "_action_handler_name")`
- Example: `"get_csv_content": (None, "_action_get_csv_content"),` for public action
- Example: `"save_col_widths": (EDIT_ROLES, "_action_save_col_widths"),` for restricted action
- List all GET actions in CONTEXT.md (12+): get_rules, get_csvs, get_csv_content, get_mapping, get_versions, check_csv_status, report_presence, get_col_widths, get_apps, check_daily_limit_status, get_pending_approvals, get_request_csv, get_notifications
- Use `None` for required_roles if no RBAC check needed (public actions)
- Use role sets (ADMIN_ROLES, EDIT_ROLES, VIEWER_ROLES) for restricted actions

POST_ACTIONS dict: same structure, 15+ actions
- Include all POST actions from current handler (save_csv, create_csv, process_approval, etc.)
- Will implement only simple POST actions in Wave 2; complex ones stubbed in Wave 3

**Shared _dispatch() method:**
```python
def _dispatch(self, table, action, request, user, roles, payload=None, query=None):
    # Check: user != "unknown" → 401
    # Check: action in table → 400 Unknown action
    # Extract required_roles, handler_name from table[action]
    # Check: required_roles and not roles.intersection(required_roles) → 403
    # Call handler = getattr(self, handler_name)
    # Call handler(request, payload or query, user, roles)
    # Log access with _make_access_log(action, user, status_code, duration_ms, payload_bytes)
    # Return response
```

**handle() method refactor:**
```python
def handle(self):
    # Parse request: method (GET/POST), action from query/body
    # Extract user, roles from session
    # Call _dispatch(GET_ACTIONS or POST_ACTIONS, action, ...)
    # Return response
```

**GET handlers (_action_* methods):**
Each handler signature: `def _action_X(self, request, query, user, roles) -> dict`
- Parse query params (csv_file, version_id, rule_name, etc.)
- Call domain module functions directly (e.g., read_csv, get_versions_list, etc.)
- Validate preconditions (CSV exists, etc.)
- Handle exceptions: ValueError→400, FileNotFoundError→404, PermissionError→403, IOError→500
- Return {field: value, ...} for success; never return {success: true} for GET (only POST)
- Example for get_csv_content: return {csv_file, rows, columns, mtime}

**Dispatch completeness check:**
After handler rewrite, verify:
- All _action_* methods in class are referenced in GET_ACTIONS or POST_ACTIONS
- All entries in GET_ACTIONS and POST_ACTIONS resolve to _action_* methods via getattr()
- No orphaned handlers or stale table entries

**Preserve existing code:**
- _resp() method unchanged
- _parse_query() unchanged
- Any other helper methods needed by handlers
- Keep all constants and imports from current handler

**POST_ACTIONS stubs for now:**
- Include all POST action entries in the table
- Handlers can be stubbed with `raise NotImplementedError` (will be implemented in Wave 2-3)
- OR point to old inline code (and refactor in-place during Wave 2-3)

**Error handling in dispatch:**
Layer errors in _dispatch():
- ValueError → 400
- FileNotFoundError → 404
- PermissionError → 403
- IOError → 500
- Exception catch-all → 500

**Access logging:**
_make_access_log() returns JSON-serializable dict: {type, action, user, method, status, duration_ms, payload_bytes, ts}
Write to audit logger with `logger.info(json.dumps(access_log))`

**Comments and docstrings:**
- Docstring on each handler explaining its purpose
- Inline comments on dispatch tables grouping by domain (CSV operations, rule operations, approval operations, etc.)
- Module-level comment explaining dispatch table pattern
  </action>
  <verify>
    <automated>grep -n "class WhitelistHandler" bin/wl_handler.py && grep -n "GET_ACTIONS = {" bin/wl_handler.py && grep -n "POST_ACTIONS = {" bin/wl_handler.py && grep -n "def _dispatch" bin/wl_handler.py && grep -n "def _action_get_" bin/wl_handler.py | wc -l && python -m py_compile bin/wl_handler.py</automated>
  </verify>
  <done>
wl_handler.py refactored with GET_ACTIONS and POST_ACTIONS dispatch tables at class level; shared _dispatch() method implemented; 10+ GET handlers as _action_* methods (stubbed POST handlers included in table but not implemented); handle() delegates to _dispatch(); module compiles without syntax errors; all _action_* methods referenced in dispatch tables.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for dispatch table and GET handlers</name>
  <files>tests/integration/test_handler_dispatch.py, tests/unit/test_replay.py</files>
  <read_first>
    - tests/conftest.py (fixtures: mock_request, mock_session, mock_roles)
    - bin/wl_handler.py (dispatch tables, GET_ACTIONS, POST_ACTIONS)
    - bin/wl_replay.py (execute_approved_action signature)
    - 04-CONTEXT.md "Integration Test Strategy" section
  </read_first>
  <action>
Create tests/integration/test_handler_dispatch.py and tests/unit/test_replay.py.

**tests/integration/test_handler_dispatch.py (100+ lines):**

1. `test_dispatch_tables_complete()`:
   - Verify all _action_* methods in WhitelistHandler are in GET_ACTIONS or POST_ACTIONS
   - Verify all table entries resolve to methods via getattr(handler, method_name)
   - Verify no None method names in tables
   - Assert len(orphaned_methods) == 0, len(unresolved_entries) == 0

2. `test_get_action_dispatch(handler, action_name, user, expected_status)`:
   - Parametrized: (get_csv_content, analyst, 200), (get_rules, analyst, 200), (get_versions, unknown_user, 401), (unknown_action, analyst, 400), (save_csv, viewer, 403)
   - Call handler._dispatch(GET_ACTIONS, action_name, request, user, roles, query=query_dict)
   - Assert response status == expected_status
   - Assert "error" key in response for 4xx/5xx; no "error" for 2xx

3. `test_dispatch_session_expired()`:
   - Call _dispatch(..., user="unknown", ...)
   - Assert 401 response with "Session expired" error

4. `test_dispatch_unknown_action()`:
   - Call _dispatch(..., action="unknown_action", ...)
   - Assert 400 response with "Unknown action" error

5. `test_dispatch_rbac_check()`:
   - Call _dispatch with action="save_csv" (requires EDIT_ROLES), roles={} (viewer, no edit)
   - Assert 403 response with "Permission denied" error

6. `test_get_csv_content()`:
   - Mock domain modules (wl_csv.read_csv returns test data)
   - Call _action_get_csv_content(request, query={csv_file: "test.csv"}, user, roles)
   - Assert response contains {csv_file, rows, columns, mtime}
   - Verify mtime matches expected value

7. `test_get_versions_list()`:
   - Mock wl_versions.get_versions_list returns [version_1, version_2, ...]
   - Call _action_get_versions(..., csv_file="test.csv")
   - Assert response contains {versions: [...]}

8. `test_missing_csv_file()`:
   - Query param missing csv_file for action requiring it
   - Assert 400 error with validation message

9. `test_handler_exception_handling()`:
   - Mock domain module to raise FileNotFoundError
   - Call _action_get_csv_content
   - Assert 404 response

10. `test_access_log_structure()`:
    - Mock audit logger
    - Call _dispatch for any action
    - Assert access log written with {type, action, user, method, status, duration_ms, payload_bytes, ts}
    - Assert JSON serializable

**tests/unit/test_replay.py (80+ lines):**

1. `test_execute_approved_action_save_csv()`:
   - Mock save_csv_pipeline returns (True, "Success")
   - Create context dict and request_item dict
   - Call execute_approved_action(context, request_item)
   - Assert {success: True, message: "..."}
   - Verify post_audit_event called

2. `test_execute_approved_action_missing_csv()`:
   - Request item has action="save_csv", csv_file="missing.csv"
   - Mock os.path.exists returns False for csv_file
   - Call execute_approved_action
   - Assert {success: False, error_type: "missing_csv", error: "CSV file no longer exists"}

3. `test_execute_approved_action_unknown_type()`:
   - Request item has action_type="unknown_action"
   - Call execute_approved_action
   - Assert {success: False, error: "Unknown action type"}

4. `test_replay_handler_dispatch()`:
   - For each action type in REPLAY_HANDLERS (save_csv, revert_csv, create_rule, delete_rule, delete_csv, create_csv)
   - Verify handler exists and is callable
   - Assert len(missing_handlers) == 0

5. `test_execute_replay_save_csv_failure()`:
   - Mock save_csv_pipeline returns (False, "File locked")
   - Call _execute_replay_save_csv
   - Assert {success: False, error: "File locked"}
   - Verify audit not posted on primary failure

6. `test_execute_replay_audit_failure()`:
   - Mock save_csv_pipeline returns (True, "Success")
   - Mock post_audit_event raises Exception
   - Call _execute_replay_save_csv
   - Assert {success: True, ...} (audit failure is non-blocking)
   - Verify exception logged but doesn't fail the operation

7. `test_replay_context_structure()`:
   - Create context dict with required keys: original_analyst, approving_admin, is_dual_admin, request_id, action_type, session_key, approved_at
   - Call execute_approved_action
   - Verify context passed correctly to handlers

**Test fixtures (in conftest.py or test file):**
- mock_handler: WhitelistHandler instance with mocked dependencies
- mock_request: fake HTTP request object
- mock_session: {user: "analyst", roles: {"viewer"}}
- mock_context: {original_analyst: "analyst", approving_admin: "admin", ...}

**Mock strategy:**
- Use @patch decorator to mock domain modules (wl_csv, wl_versions, wl_rules, etc.)
- Mock audit logger to verify access logs
- Use freezegun for consistent timestamps
- No Docker container required for unit/integration tests
  </action>
  <verify>
    <automated>python -m pytest tests/integration/test_handler_dispatch.py -v && python -m pytest tests/unit/test_replay.py --cov=bin.wl_replay --cov-fail-under=80 -v</automated>
  </verify>
  <done>
tests/integration/test_handler_dispatch.py exists with 10+ test functions covering dispatch table completeness, action dispatch with RBAC, error handling, and access logging; tests/unit/test_replay.py exists with 7+ test functions covering all replay action types, precondition validation, audit posting, and context passing; all tests pass.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. `wl_replay.py` compiles and has execute_approved_action as entry point
2. `wl_handler.py` has GET_ACTIONS and POST_ACTIONS as class-level dicts
3. Dispatch table entries resolve to _action_* methods via getattr()
4. All GET handlers (_action_get_*) implemented
5. test_handler_dispatch.py tests pass: dispatch completeness, RBAC, error handling, access logging
6. test_replay.py tests pass: all replay action handlers, precondition checks, audit integration
7. No syntax errors in any Python file
8. wl_replay.py is importable and exports execute_approved_action
9. Handler imports wl_replay and other Phase 1-3 modules without circular imports
10. Access log structure is JSON-serializable

**Final check:** Run full test suite: `python -m pytest tests/unit tests/integration -v --tb=short` — expect 380+ tests passing (all Phase 1-3 tests + 20+ new tests)
</verification>

<success_criteria>
- wl_replay.py exists with 150+ lines, executes approved actions via domain module pipelines
- wl_handler.py refactored with GET_ACTIONS and POST_ACTIONS dispatch tables
- All 10+ GET handlers implemented as _action_* methods
- shared _dispatch() method routes requests correctly
- Dispatch table completeness tests pass (all handlers in tables, all entries resolve)
- GET handler integration tests pass (RBAC, error handling, query parsing)
- Replay action handlers tested with mocked domain modules
- Zero functional change to existing API — all GET actions return same data as before
- All tests passing: 380+ total (Phase 1-3 + 20+ new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-integration/04-01-SUMMARY.md` with:
- wl_replay.py: Layer 5 module, 150+ lines, 7 exported functions, REPLAY_HANDLERS dict
- wl_handler.py: Refactored GET handlers (Wave 1), dispatch tables, shared _dispatch()
- Tests: 20+ new test functions covering dispatch, GET handlers, replay actions
- Status: Wave 1 COMPLETE, ready for Wave 2 (simple POST handlers)
</output>
