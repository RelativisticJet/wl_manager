---
phase: 05-frontend-architecture
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - appserver/static/whitelist_manager.js
  - appserver/static/modules/wl_orchestrator.js
  - tests/qunit/test_module_loading.js
  - tests/qunit/test_state_transitions.js
  - default/app.conf
autonomous: false
requirements: [TEST-05]

must_haves:
  truths:
    - Entry point orchestrates all module initialization in dependency order
    - All cross-module communication works via State events and custom jQuery events
    - QUnit tests verify module loading order and state transitions
  artifacts:
    - path: appserver/static/whitelist_manager.js
      provides: Thin entry point orchestrator (~100 lines)
      exports: "Entry point initialization"
      min_lines: 100
    - path: appserver/static/modules/wl_orchestrator.js
      provides: Cross-module workflow orchestration
      exports: "init, orchestrateWorkflows"
      min_lines: 200
    - path: tests/qunit/test_module_loading.js
      provides: Module loading order tests
      min_lines: 80
    - path: tests/qunit/test_state_transitions.js
      provides: State transition tests
      min_lines: 120
  key_links:
    - from: whitelist_manager.js
      to: all feature modules
      via: require() calls in dependency order
      pattern: "require.*\\[.*modules/wl"
    - from: modules/wl_orchestrator.js
      to: all feature modules
      via: cross-module event wiring
      pattern: "on.*wl:.*Updated"
    - from: feature modules
      to: modules/wl_state.js
      via: State.get(), State.set(), State.on()
      pattern: "State\\.(get|set|on)"

---

<objective>
**Wave 4: Finalization — Orchestrator, Testing, Verification**

Finalize frontend architecture by wiring all cross-module communication, creating comprehensive QUnit tests, and verifying all requirement coverage. Create orchestrator module to coordinate complex workflows (save, load CSV, revert, approval).

**Purpose:** Complete module extraction, ensure all features work together, achieve TEST-05 requirement coverage, verify frontend modularization complete.

**Output:**
- Entry point slim to ~100 lines (foundation + orchestration only)
- `wl_orchestrator.js` module for complex workflows
- Comprehensive QUnit tests for module loading and state transitions
- Verification that all FMOD requirements addressed
- Clean git commit with all Phase 5 work
</objective>

<execution_context>
@C:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-architecture/05-CONTEXT.md

## Locked Decisions from CONTEXT.md

### Entry Point Rewrite
- Reduce to ~100 lines: module initialization, URL parameter handling, basic event wiring
- No business logic, only thin wiring between modules
- Ordered init: constants → state → rest → ui → foundation modules → features
- Fail-fast: catch any init() throw and call showFatalError()

### Orchestrator Module
- Straddling functions (save, load CSV, revert) stay in entry point as thin orchestrators
- Call module APIs in sequence, no business logic
- Wire complex workflows via entry point event listeners

### Testing
- QUnit tests for module loading order (verify dependencies resolved correctly)
- QUnit tests for state transitions (verify State fires events correctly)
- Phase 5 scope: ~4 test files, ~50 assertions total

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wl_orchestrator.js for complex workflows</name>
  <files>appserver/static/modules/wl_orchestrator.js</files>
  <action>
Create `appserver/static/modules/wl_orchestrator.js` as AMD module coordinating complex workflows across feature modules.

Public API: init(), orchestrateSaveCSV(), orchestrateLoadCSV(), orchestrateRevertCSV(), orchestrateApprovalProcess().

Implementation:
- Require all feature modules and foundation modules
- orchestrateSaveCSV(): 
  - Check if bulk edit (edited count >= 2)
  - If bulk, check daily limits, show approval modal if needed
  - Call REST.restPost('save_csv', payload)
  - On success: update State with originalRows = currentRows, fire 'wl:csvSaved'
  - On conflict (409): show conflict dialog, options to override or reload
- orchestrateLoadCSV(rule, csv):
  - Load CSV via REST.restGet('get_csv')
  - Update State.batch({currentRows, originalRows, detectionRuleSelected, csvFileSelected})
  - Trigger Table.refreshTable()
  - Load versions via Versions.loadVersions()
- orchestrateRevertCSV(versionId):
  - Call Versions.revertToVersion(versionId) — let module handle UI
  - Update Table
- orchestrateApprovalProcess(action):
  - Check if requires approval (for admin users only)
  - Call ApprovalUI.showApprovalNeeded()
  - Handle approval response from backend

Entry point calls orchestrator for complex workflows, keeps simple operations in feature modules.

Acceptance: All complex workflows orchestrated, module APIs called in correct sequence, State updated correctly, error handling in place.
  </action>
  <verify>
    <automated>
cd C:\Users\PC\wl_manager && \
grep -c "orchestrateSaveCSV:" appserver/static/modules/wl_orchestrator.js && \
grep -c "orchestrateLoadCSV:" appserver/static/modules/wl_orchestrator.js && \
grep -c "orchestrateRevertCSV:" appserver/static/modules/wl_orchestrator.js
    </automated>
  </verify>
  <done>
wl_orchestrator.js module created with init(), orchestrateSaveCSV(), orchestrateLoadCSV(), orchestrateRevertCSV(), orchestrateApprovalProcess() methods. Coordinates workflows across feature modules, handles error cases, updates State correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Slim down entry point to ~100 lines orchestrator</name>
  <files>appserver/static/whitelist_manager.js</files>
  <action>
Rewrite `appserver/static/whitelist_manager.js` as thin entry point (~100 lines) containing only:
- Foundation and feature module requires (all AMD modules)
- Module initialization in dependency order: constants → state → rest → ui → features
- URL parameter parsing (rule, csv)
- CSV load on initial load
- Basic event wiring (wl:* events → orchestrator calls)
- Error handling: catch init() errors and show fatal error
- $(document).ready() handler

All complex business logic moved to wl_orchestrator.js or feature modules.

After line 100, place clear comment: "=== ALL BUSINESS LOGIC MOVED TO FEATURE MODULES AND ORCHESTRATOR ===".

Acceptance: Entry point reduced to ~100 lines, only initialization and basic wiring, no business logic, all complex workflows in orchestrator.
  </action>
  <verify>
    <automated>
cd C:\Users\PC\wl_manager && \
wc -l appserver/static/whitelist_manager.js && \
head -100 appserver/static/whitelist_manager.js | tail -5
    </automated>
  </verify>
  <done>
Entry point slimmed to ~100 lines with only foundation/feature initialization, URL parsing, event wiring, and error handling. All business logic moved to feature modules and orchestrator.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create QUnit tests for module loading order</name>
  <files>tests/qunit/test_module_loading.js</files>
  <action>
Create `tests/qunit/test_module_loading.js` with QUnit.test() assertions verifying correct module loading order and dependency resolution.

Tests to cover (~10 assertions):
- Foundation modules load before feature modules
- Feature modules load and export correct API objects
- All required methods exist on exported objects (Table.refreshTable, Search.search, etc.)
- Modules initialize without errors
- State and REST available globally/via module imports
- Circular dependency check (modules don't create circular references)
- Fast module loading on slow network (simulated with setTimeout delays)

Acceptance: 10+ assertions covering loading order and dependency resolution, tests pass.
  </action>
  <verify>
    <automated>
cd C:\Users\PC\wl_manager && \
grep -c "QUnit.test" tests/qunit/test_module_loading.js
    </automated>
  </verify>
  <done>
test_module_loading.js created with 10+ assertions verifying module loading order, dependency resolution, API exports, initialization without errors.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create QUnit tests for state transitions</name>
  <files>tests/qunit/test_state_transitions.js</files>
  <action>
Create `tests/qunit/test_state_transitions.js` with QUnit.test() assertions verifying state transitions and event firing.

Tests to cover (~15 assertions):
- State set triggers correct event with new/old values
- State batch applies all updates atomically
- isDirty() detects when currentRows differ from originalRows
- isDirty() fires 'state:dirty' event only on status change
- State reset clears all keys to defaults
- Features listening to State changes react correctly (e.g., Table refreshes on currentRows change)
- Complex workflow: load CSV → edit → isDirty() true → save → isDirty() false
- Bulk edit detection: check edited count >= 2 triggers approval
- Version revert: oldRows restored, State updated
- Approval queue state changes propagate to UI

Acceptance: 15+ assertions covering all state transitions, events, and complex workflows.
  </action>
  <verify>
    <automated>
cd C:\Users\PC\wl_manager && \
grep -c "QUnit.test" tests/qunit/test_state_transitions.js
    </automated>
  </verify>
  <done>
test_state_transitions.js created with 15+ assertions verifying state transitions, event firing, dirty detection, complex workflows, approval detection, version revert.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create test_runner.xml dashboard and verify QUnit tests pass</name>
  <files>default/data/ui/views/test_runner.xml</files>
  <action>
Create hidden Splunk dashboard `default/data/ui/views/test_runner.xml` that loads QUnit library and all test files.

Dashboard structure:
- `<label>QUnit Test Runner</label>`
- `<description>Hidden dashboard for running QUnit tests. Not visible in UI.</description>`
- HTML panel with:
  - `<div id="qunit"></div>` — QUnit output container
  - `<div id="qunit-fixture"></div>` — QUnit fixture
  - `<link rel="stylesheet" href="/static/app/wl_manager/qunit/qunit.css">`
  - `<script src="/static/app/wl_manager/qunit/qunit.js"></script>`
  - `<script>` block requiring all modules and loading test files via $.getScript()

Load test files in order:
1. test_state_manager.js
2. test_rest_helpers.js
3. test_module_loading.js
4. test_state_transitions.js

After dashboard created, verify all tests pass (navigate to dashboard in browser, check console for results).

Acceptance: Dashboard created, all QUnit tests load, tests pass without errors.
  </action>
  <verify>
    <automated>
cd C:\Users\PC\wl_manager && \
grep -c "QUnit" default/data/ui/views/test_runner.xml && \
grep -c "test_state_manager" default/data/ui/views/test_runner.xml && \
grep -c "test_state_transitions" default/data/ui/views/test_runner.xml
    </automated>
  </verify>
  <done>
test_runner.xml hidden dashboard created, loads QUnit library and all test files (test_state_manager, test_rest_helpers, test_module_loading, test_state_transitions). All QUnit tests pass.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
- wl_orchestrator.js module coordinating complex workflows
- Entry point slimmed to ~100 lines
- 4 QUnit test files with 50+ total assertions
- test_runner.xml hidden dashboard
  </what-built>
  <how-to-verify>
1. Navigate to `https://localhost:8000/app/wl_manager/whitelist_manager`
2. Verify page loads without JavaScript errors (check browser console)
3. Verify whitelist table displays with content
4. Test save → approval flow for bulk edit (edit 2+ rows, click save, see approval modal)
5. Test version revert: click versions dropdown, select a version, confirm revert
6. Navigate to `https://localhost:8000/app/wl_manager/test_runner`
7. Verify QUnit test dashboard loads
8. Verify all 4 test files load (should see test names in QUnit output)
9. Verify all 50+ assertions pass (green checkmarks in QUnit output)
10. Test complex workflow: load CSV → edit row → see isDirty true → save → check originalRows updated
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass and features work, or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Verify requirement coverage and create comprehensive SUMMARY</name>
  <files>default/app.conf</files>
  <action>
Verify that all Phase 5 requirements (FMOD-01 through FMOD-05, FMOD-08, TEST-05) are addressed by plans 05-01 through 05-04.

Requirement checklist:
- [ ] FMOD-01: whitelist_manager.js rewritten as thin AMD entry point (~100 lines) ✓ (05-01 rewrite + 05-04 finalization)
- [ ] FMOD-02: wl_constants.js extracts all magic numbers, selectors, patterns ✓ (05-01)
- [ ] FMOD-03: wl_rest.js provides shared REST helpers (restGet, restPost) ✓ (05-01)
- [ ] FMOD-04: wl_state.js implements state manager with events and validation ✓ (05-01)
- [ ] FMOD-05: Feature modules extracted: wl_table, wl_search, wl_modals, wl_versions, wl_approval_ui, wl_csv_io, wl_presence ✓ (05-02 independent + 05-03 coupled)
- [ ] FMOD-08: notifications.js refactored to use wl_rest.js ✓ (05-01)
- [ ] TEST-05: Browser E2E tests for key workflows (load CSV, save, approve, revert) — QUnit infrastructure ✓ (05-01 + 05-04 comprehensive tests)

Increment build number in app.conf one final time.

Create comprehensive SUMMARY.md documenting:
- Phase 5 overview (4 plans, 11 feature modules extracted)
- Plans overview (05-01 foundation, 05-02 independent, 05-03 coupled, 05-04 finalization)
- Module directory structure
- Requirement coverage matrix (all FMOD-01 through FMOD-05, FMOD-08, TEST-05 addressed)
- Deployment checklist
- Known limitations/future work (Phase 6 control_panel, Phase 7 E2E browser tests)

Acceptance: All requirements addressed, SUMMARY.md created with full documentation.
  </action>
  <verify>
    <automated>
cd C:\Users\PC\wl_manager && \
grep "^build = " default/app.conf && \
test -f .planning/phases/05-frontend-architecture/05-SUMMARY.md && \
grep -c "FMOD-01" .planning/phases/05-frontend-architecture/05-SUMMARY.md
    </automated>
  </verify>
  <done>
All Phase 5 requirements (FMOD-01, FMOD-02, FMOD-03, FMOD-04, FMOD-05, FMOD-08, TEST-05) verified as addressed. Build number incremented. Comprehensive SUMMARY.md created documenting phase overview, all 4 plans, 11 feature modules, requirement coverage, deployment checklist, future work.
  </done>
</task>

</tasks>

<verification>
**Plan 05-04 (Wave 4: Finalization) Completion Checklist:**

Orchestrator created:
- [ ] wl_orchestrator.js with complex workflow coordination
- [ ] orchestrateSaveCSV, orchestrateLoadCSV, orchestrateRevertCSV, orchestrateApprovalProcess

Entry point finalized:
- [ ] whitelist_manager.js reduced to ~100 lines
- [ ] Only initialization, URL parsing, event wiring (no business logic)
- [ ] All feature modules required and initialized

Testing complete:
- [ ] test_module_loading.js with 10+ assertions
- [ ] test_state_transitions.js with 15+ assertions
- [ ] test_runner.xml dashboard created
- [ ] All 50+ QUnit assertions pass

Requirements verified:
- [ ] FMOD-01 through FMOD-05, FMOD-08, TEST-05 all addressed
- [ ] SUMMARY.md created with full documentation

**All Phase 5 requirements complete.**
</verification>

<success_criteria>
**Plan 05-04 succeeds when:**

1. **Orchestrator created and functional**
2. **Entry point finalized as ~100-line orchestrator**
3. **QUnit tests created and passing**
4. **test_runner dashboard functional**
5. **All requirements verified as addressed**
6. **SUMMARY.md created**
7. **Checkpoint verification passes**
8. **Final git commit with all Phase 5 work**

**Plan 05-04 is DONE when all criteria met and git commit created.**
</success_criteria>

<output>
After completion, create comprehensive `.planning/phases/05-frontend-architecture/05-SUMMARY.md` documenting:
- Phase 5 overview and goals achieved
- Plans overview (4 plans, 4 waves)
- 11 feature modules extracted with file sizes and exports
- Module dependency graph
- Requirement coverage matrix (FMOD-01 through FMOD-05, FMOD-08, TEST-05)
- Deployment checklist completion
- Manual smoke test results
- QUnit test results (pass rate, assertions)
- Known limitations
- Next steps: Phase 6 (Control Panel Modularization)
</output>
