---
phase: 06-admin-panel
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - appserver/static/modules/wl_cp_modals.js
  - appserver/static/control_panel.js
  - default/app.conf
autonomous: true
gap_closure: true
requirements: [FMOD-06]
must_haves:
  truths:
    - "control_panel.js is a thin entry point under 200 lines"
    - "Modal helper factory (createOverlay, createModal, showCpAlert, showCpConfirm, showCpPrompt) is extracted to shared wl_cp_modals.js module"
    - "All control_panel.js modules import and use the shared modal helpers without duplication"
  artifacts:
    - path: "appserver/static/modules/wl_cp_modals.js"
      provides: "Reusable modal helper factory functions (AMD module)"
      exports: "[createOverlay, createModal, showCpAlert, showCpConfirm, showCpPrompt]"
    - path: "appserver/static/control_panel.js"
      provides: "Thin AMD entry point for control panel"
      max_lines: 200
    - path: "default/app.conf"
      provides: "Build cache-busting (increment from 490 to 491)"
  key_links:
    - from: "control_panel.js"
      to: "wl_cp_modals.js"
      via: "AMD require(['modules/wl_cp_modals'])"
      pattern: "require.*modules/wl_cp_modals"
    - from: "wl_cp_modals.js"
      to: "context object"
      via: "Functions passed to modules via ctx.showAlert/showConfirm/showPrompt"
      pattern: "ctx\\.(showAlert|showConfirm|showPrompt)"

---

<objective>
Extract modal helper implementations from control_panel.js into a shared reusable AMD module, reducing entry point from 340 lines to ~180-200 lines target and establishing reusable modal infrastructure (FMOD-06 requirement).

Purpose: Control panel entry point should orchestrate features, not implement helpers. Modal helpers are shared infrastructure that future features can reuse.

Output: wl_cp_modals.js (new module), refactored control_panel.js (smaller), cache-busted app.conf
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-admin-panel/06-CONTEXT.md (user decisions on modal helpers, reusability, dependency injection)
@appserver/static/modules/wl_rest.js (pattern reference for AMD module structure)
@appserver/static/modules/wl_ui.js (pattern reference for theme-aware DOM creation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract modal helper factory to wl_cp_modals.js</name>
  <files>appserver/static/modules/wl_cp_modals.js</files>
  <read_first>
    - appserver/static/control_panel.js (lines 65-156: modal factory implementations and helper functions)
  </read_first>
  <action>
Create new AMD module `appserver/static/modules/wl_cp_modals.js` (~100 lines) with extracted modal factory functions. This module should:

1. Define a factory function `createOverlay()` that builds a dark overlay div with fixed positioning, semi-transparent black background, centered flex layout, and z-index 10000. Mark it with class `wl-modal-overlay` so queue's modal guard (`$(".wl-modal-overlay").length`) works correctly.

2. Define a factory function `createModal(isDark)` that builds a modal div with:
   - Background color: dark theme uses `#2c2e31`, light uses `#fff`
   - Text color: dark uses `#e0e0e0`, light uses `#333`
   - Border: `1px solid` with dark/light variants (`#444` / `#ddd`)
   - Border radius, box shadow, padding, max/min width as per lines 74-80 of control_panel.js
   - Class `wl-cp-modal`

3. Define `showCpAlert(title, message)` that returns a Promise resolving on OK:
   - Detect dark theme with `$("body").hasClass("wl-dark")`
   - Create overlay + modal
   - Append h2 title, p message, OK button
   - Button click fades out overlay, removes it, resolves promise
   - Code: adapt lines 83-97 of control_panel.js

4. Define `showCpConfirm(title, message, opts)` that returns a Promise:
   - opts defaults to `{}`
   - Detect dark theme
   - Create overlay + modal
   - Append h2 title, p message
   - Append button div with 8px gap containing Cancel and OK buttons
   - Cancel button: resolves false, OK resolves true
   - Text labels from opts.cancelLabel/opts.okLabel (defaults: "Cancel"/"OK")
   - Code: adapt lines 99-122 of control_panel.js

5. Define `showCpPrompt(title, message, placeholder)` that returns a Promise:
   - Create text input with:
     - Placeholder from parameter (or empty string)
     - Width 100%, padding 8px, margin 10px 0 15px 0
     - Background: dark `#1a1c20`, light `#f5f5f5`
     - Color: dark `#e0e0e0`, light `#333`
     - Border: 1px solid with dark/light variants, radius 4px, box-sizing border-box
   - Create overlay + modal with h2 title, p message, input, button div
   - Button div: Cancel resolves null, OK resolves input.val()
   - Auto-focus input on display
   - Code: adapt lines 124-156 of control_panel.js

6. Export all 5 functions via AMD define() pattern:
   ```javascript
   define(function () {
       "use strict";
       return {
           createOverlay: createOverlay,
           createModal: createModal,
           showCpAlert: showCpAlert,
           showCpConfirm: showCpConfirm,
           showCpPrompt: showCpPrompt
       };
   });
   ```

Copy all inline implementations from control_panel.js lines 65-156 exactly as-is into the new module functions. Do NOT modify logic, only structure for AMD export.
  </action>
  <verify>
    <automated>grep -c "define(function" appserver/static/modules/wl_cp_modals.js</automated>
  </verify>
  <acceptance_criteria>
    - File appserver/static/modules/wl_cp_modals.js exists
    - File has exactly 1 `define(function () {` call (AMD pattern)
    - File exports 5 functions: createOverlay, createModal, showCpAlert, showCpConfirm, showCpPrompt
    - Each exported function has exact logic from control_panel.js lines 65-156 (copy-paste, no modifications)
    - createOverlay() sets class "wl-modal-overlay" (verified: `.addClass("wl-modal-overlay")` present)
    - createModal() sets class "wl-cp-modal" (verified: `.addClass("wl-cp-modal")` present)
    - showCpAlert, showCpConfirm, showCpPrompt return Promises
    - Dark theme detection uses `$("body").hasClass("wl-dark")`
  </acceptance_criteria>
  <done>wl_cp_modals.js created, 5 modal factory functions exported, exact line-by-line copy from control_panel.js, no logic changes</done>
</task>

<task type="auto">
  <name>Task 2: Refactor control_panel.js to import and use shared modal helpers</name>
  <files>appserver/static/control_panel.js</files>
  <read_first>
    - appserver/static/control_panel.js (entire file, to verify refactoring)
    - appserver/static/modules/wl_cp_modals.js (newly created module, to verify AMD export)
  </read_first>
  <action>
Refactor control_panel.js to import wl_cp_modals module and remove inline implementations. This should reduce control_panel.js from 340 lines to ~180-200 lines target.

1. Update AMD require() at top of file (lines 14-26):
   - Add `"modules/wl_cp_modals"` to require array (after wl_constants, before wl_cp_queue)
   - Add `Modals` parameter to callback function: `function ($, _, mvc, REST, UI, Constants, Modals, QueueModule, ...)`

2. Delete lines 65-156 (entire modal helper factory implementations):
   - Remove `function createOverlay() { ... }`
   - Remove `function createModal(isDark) { ... }`
   - Remove `function showCpAlert(title, message) { ... }`
   - Remove `function showCpConfirm(title, message, opts) { ... }`
   - Remove `function showCpPrompt(title, message, placeholder) { ... }`

3. Update lines that reference modal helpers to use imported versions:
   - Line 312 (old, now deleted): `showAlert: showCpAlert,` becomes `showAlert: Modals.showCpAlert,`
   - Line 313: `showConfirm: showCpConfirm,` becomes `showConfirm: Modals.showCpConfirm,`
   - Line 314: `showPrompt: showCpPrompt,` becomes `showPrompt: Modals.showCpPrompt,`

4. Keep lines 256-308 (notification system: showNewRequestsToast, updateQueueBadge) as-is:
   - These are control_panel-specific (not reusable), acceptable to keep inline
   - The verifier noted these 52 lines as INFO-level bloat, but necessary for notification badge/toast

5. Final control_panel.js structure should be:
   - Lines 1-27: AMD require() with wl_cp_modals added
   - Lines 28-44: User detection and role extraction (cpCurrentUser, cpIsSuperAdmin, cpIsAdmin)
   - Lines 45-56: Access control gate (get_approval_queue check)
   - Lines 57-260: initCP() function containing:
     - Theme detection (5 lines)
     - Tab routing (showTab function, ~50 lines) — UNCHANGED
     - Tab rendering (tabs array + forEach, ~15 lines) — UNCHANGED
     - Tab click handler (lines 225-227) — UNCHANGED
     - Initial tab from URL (lines 229-230) — UNCHANGED
     - Browser visibility handler (lines 233-251) — UNCHANGED
     - Notification system (showNewRequestsToast, updateQueueBadge, ~52 lines) — UNCHANGED
     - Context injection using Modals exports (showAlert/showConfirm/showPrompt now reference Modals.*)
     - Module initialization (lines 321-335) — UNCHANGED
   - Total after deletion of modal helpers: ~180-200 lines target

6. Verify no references to createOverlay, createModal, showCpAlert, showCpConfirm, showCpPrompt outside of the deleted section. The only references should now be in the ctx object using Modals.*.

Exact replacements:
- OLD: `showAlert: showCpAlert,` → NEW: `showAlert: Modals.showCpAlert,`
- OLD: `showConfirm: showCpConfirm,` → NEW: `showConfirm: Modals.showCpConfirm,`
- OLD: `showPrompt: showCpPrompt,` → NEW: `showPrompt: Modals.showCpPrompt,`
  </action>
  <verify>
    <automated>wc -l appserver/static/control_panel.js</automated>
  </verify>
  <acceptance_criteria>
    - control_panel.js has ≤200 lines (target ≤180-200; verify via `wc -l`)
    - AMD require includes "modules/wl_cp_modals" string in array
    - AMD callback has `Modals` parameter
    - Lines 65-156 (modal helper functions) are completely deleted
    - ctx object references use `Modals.showCpAlert`, `Modals.showCpConfirm`, `Modals.showCpPrompt`
    - No references to createOverlay, createModal, showCpAlert (raw), showCpConfirm (raw), showCpPrompt (raw) remain in file
    - Lines 256-308 (notification system) are UNCHANGED and still present
    - All 5 CP modules (QueueModule, LimitsModule, TrashModule, AdminLimitsModule, UsageModule) are still imported and initialized
    - Tab routing, browser visibility handler, and module initialization logic is UNCHANGED
  </acceptance_criteria>
  <done>control_panel.js refactored to import wl_cp_modals, inline modal implementations deleted, entry point reduced to ~180-200 lines, context object uses shared modal helpers</done>
</task>

<task type="auto">
  <name>Task 3: Bump app.conf build number for cache busting</name>
  <files>default/app.conf</files>
  <read_first>
    - default/app.conf (to verify current build number)
  </read_first>
  <action>
Update default/app.conf to increment build number from 490 to 491 for Splunk static asset cache busting (JS/CSS changes require build bump + i18n cache clear + restart per CLAUDE.md deployment notes).

Locate the `[ui]` section in app.conf and find the `build = ` line. Change:
- OLD: `build = 490`
- NEW: `build = 491`

This is the 4th plan of Phase 6 (after 06-01 → 488, 06-02 → 489, 06-03 → 490), so plan 06-05 increments to 491.
  </action>
  <verify>
    <automated>grep "^build = " default/app.conf</automated>
  </verify>
  <acceptance_criteria>
    - default/app.conf [ui] section has `build = 491`
    - Old build = 490 is not present
  </acceptance_criteria>
  <done>app.conf build number incremented to 491</done>
</task>

</tasks>

<verification>
**Phase Gap Closure Verification**

After all tasks complete, verify gap is closed:

1. **Line count reduction** — control_panel.js must be ≤200 lines:
   ```bash
   wc -l appserver/static/control_panel.js
   # Expected: ≤200 lines
   ```

2. **Modal module exists and is valid AMD** — wl_cp_modals.js:
   ```bash
   grep -c "define(function" appserver/static/modules/wl_cp_modals.js
   # Expected: 1 (single AMD module)
   
   grep -E "showCpAlert|showCpConfirm|showCpPrompt|createOverlay|createModal" appserver/static/modules/wl_cp_modals.js | wc -l
   # Expected: ≥5 (each function defined at least once)
   ```

3. **control_panel.js imports and uses shared module**:
   ```bash
   grep "modules/wl_cp_modals" appserver/static/control_panel.js
   # Expected: "modules/wl_cp_modals" string in require array
   
   grep -E "Modals\.(showCpAlert|showCpConfirm|showCpPrompt)" appserver/static/control_panel.js | wc -l
   # Expected: ≥3 (at least 3 references in ctx object)
   ```

4. **Duplicate modal helpers removed**:
   ```bash
   grep -c "function createOverlay" appserver/static/control_panel.js
   # Expected: 0 (helper functions no longer inline)
   
   grep -c "function showCpAlert" appserver/static/control_panel.js
   # Expected: 0
   ```

5. **Build number updated**:
   ```bash
   grep "^build = " default/app.conf
   # Expected: build = 491
   ```

**Verification passes when:**
- ✓ control_panel.js ≤200 lines
- ✓ wl_cp_modals.js exists, is valid AMD module with 5 exported functions
- ✓ control_panel.js imports wl_cp_modals and uses Modals.* references
- ✓ No duplicate modal helper definitions remain in control_panel.js
- ✓ app.conf build = 491
</verification>

<success_criteria>
**Gap Closure Complete when:**
1. control_panel.js is reduced to ≤200 lines (from 340 lines)
2. Modal helper factory extracted to reusable wl_cp_modals.js AMD module
3. control_panel.js imports and uses shared modal helpers via Modals.* references
4. All tests continue to pass (test files reference context object functions, which still work via Modals import)
5. app.conf build number bumped for cache busting
6. FMOD-06 requirement satisfied: "control_panel.js is a thin AMD entry point (~150-200 lines) with shared modal helper infrastructure established"

**Requirement Mapping:**
- FMOD-06: ✓ control_panel.js thin entry point, modal helpers extracted to shared module
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-05-SUMMARY.md` documenting:
- Tasks completed (3/3)
- Line count reduction: 340 → ≤200 lines achieved
- wl_cp_modals.js module created with 5 modal factory functions
- control_panel.js refactored to import and use shared helpers
- All tests passing, no functional changes
- Build number bumped for deployment
- FMOD-06 gap closed
</output>
