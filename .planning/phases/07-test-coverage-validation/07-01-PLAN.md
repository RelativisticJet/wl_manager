---
phase: 07-test-coverage-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/pytest.ini
  - tests/unit/conftest.py
  - tests/unit/test_limits.py
  - requirements-dev.txt
autonomous: true
requirements: [TEST-01, TEST-06]

must_haves:
  truths:
    - "Unit test suite runs offline (no Docker required)"
    - "All backend modules have ≥80% code coverage"
    - "test_limits.py passes with 0 failures (clean baseline)"
    - "Conftest fixtures are consolidated with no duplication"
  artifacts:
    - path: tests/pytest.ini
      provides: "Centralized test configuration with markers"
      contains: "pytest"
    - path: tests/conftest.py
      provides: "Global session-scoped fixtures (temp dirs, Docker fixture stubs)"
      min_lines: 50
    - path: tests/unit/conftest.py
      provides: "Unit-specific fixtures (mock Splunk SDK, handler factory)"
      min_lines: 30
    - path: requirements-dev.txt
      provides: "Development dependencies (pytest, pytest-cov, freezegun, hypothesis, pytest-timeout)"
      contains: ["pytest", "pytest-cov", "freezegun", "hypothesis", "pytest-timeout"]
  key_links:
    - from: tests/conftest.py
      to: tests/unit/conftest.py
      via: "Conftest hierarchy (no duplication of fixtures)"
      pattern: "@pytest.fixture|def .*()"
    - from: tests/unit/test_*.py
      to: bin/wl_*.py
      via: "Unit test coverage targeting all backend modules"
      pattern: "import wl_|from bin import"
    - from: tests/pytest.ini
      to: tests/unit/test_*.py
      via: "Marker registration (@pytest.mark.docker, @pytest.mark.slow)"
      pattern: "markers ="
---

<objective>
Fix 10 failing test_limits.py tests, consolidate conftest infrastructure, and establish a clean test baseline for Phase 7.

Purpose: Phase 7 tests need a foundation with zero failures and clear marker registration. Consolidating conftest eliminates duplication and makes fixture scoping explicit.
Output: All unit tests pass (including test_limits.py), conftest is DRY, requirements-dev.txt has all tools needed for Phase 7.
</objective>

<execution_context>
@C:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-test-coverage-validation/07-CONTEXT.md
@.planning/phases/07-test-coverage-validation/07-RESEARCH.md

## Backend module structure (test targets)
- bin/wl_constants.py — Layer 0 (constants)
- bin/wl_logging.py — Layer 1 (logging)
- bin/wl_validation.py — Layer 2 (validation)
- bin/wl_ratelimit.py — Layer 2 (rate limiting)
- bin/wl_rbac.py — Layer 2 (RBAC)
- bin/wl_presence.py — Layer 2 (presence tracking)
- bin/wl_csv.py — Layer 3 (CSV operations)
- bin/wl_rules.py — Layer 3 (rules registry)
- bin/wl_trash.py — Layer 3 (trash operations)
- bin/wl_versions.py — Layer 3 (versioning)
- bin/wl_audit.py — Layer 3 (audit events)
- bin/wl_filelock.py — Layer 4 (file locking)
- bin/wl_limits.py — Layer 4 (daily limits) — TARGET FOR FIX
- bin/wl_approval.py — Layer 4 (approval queue)
- bin/wl_notify.py — Layer 4 (notifications)
- bin/wl_replay.py — Layer 5 (replay dispatcher)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_limits.py failing tests (10 failures)</name>
  <files>tests/unit/test_limits.py, bin/wl_limits.py</files>
  <read_first>
    - tests/unit/test_limits.py (current failing state)
    - bin/wl_limits.py (implementation to understand failures)
    - tests/unit/conftest.py (existing mock patterns)
  </read_first>
  <action>
Run `python -m pytest tests/unit/test_limits.py -v` to identify the 10 failing tests. Analyze each failure:
- Missing mock setup (e.g., unconfigured Splunk SDK mock)
- Incorrect state setup (e.g., fixture initialization)
- Logic bugs in test assertion or wl_limits.py implementation
- Timezone-related failures (ensure freezegun is used consistently)

For each test, determine if issue is in:
1. Test code (mock setup, fixtures, assertions)
2. Implementation code (wl_limits.py logic bug)
3. Test isolation (state leaking between tests)

Fixes should follow these patterns:
- Use freezegun.freeze_time() decorator/context for time-dependent tests
- Use unittest.mock.patch() for Splunk SDK calls
- Ensure reset_limits() or module-level state cleanup called between tests
- Verify zero/unlimited semantics (0=disabled, -1=unlimited) are consistent across all checks

After fixing each test, re-run to verify. Target: all 10 tests passing.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_limits.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_limits.py -v` exits 0
    - All 10 previously-failing tests now pass
    - No regressions in other tests (run full suite: `python -m pytest tests/unit/ -q`)
  </acceptance_criteria>
  <done>test_limits.py has 0 failures and ≥80% coverage on wl_limits.py</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate conftest.py and conftest.py (remove duplication)</name>
  <files>tests/conftest.py, tests/unit/conftest.py</files>
  <read_first>
    - tests/conftest.py (current global fixtures)
    - tests/unit/conftest.py (current unit-specific fixtures)
  </read_first>
  <action>
Review both files and identify duplicated fixtures or imports. Structure should be:

**tests/conftest.py (global, session-scoped):**
- temp_dir fixture: Creates temp directory for all tests
- docker_service fixture: Mock/stub for Docker presence check (session-scoped, can be skipped if container not running)
- Imports: All standard pytest plugins

**tests/unit/conftest.py (unit-specific, function-scoped):**
- mock_splunk_sdk: Patches splunk.rest to use tests/stubs/splunk/rest.py mocks
- mock_splunk_handler: Factory for creating mock wl_handler.py with mocked Splunk calls
- reset_state: Fixture that resets module-level state (e.g., presence dicts, usage trackers)

Consolidation rules:
- Keep session-scoped fixtures in conftest.py (Docker, temp dirs)
- Keep function-scoped fixtures in unit/conftest.py (mocks, state resets)
- Eliminate any fixture appearing in both files (keep in appropriate scope)
- Add docstrings to all fixtures explaining scope and purpose

After consolidation, run `python -m pytest tests/unit/ -q` to verify no breakage.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/ -q --tb=short 2>&1 | grep -E "passed|failed|ERROR"</automated>
  </verify>
  <acceptance_criteria>
    - No fixture imported in both conftest.py and tests/unit/conftest.py
    - All fixtures have scope annotation (@pytest.fixture(scope='session'|'function'))
    - All fixtures have docstrings explaining purpose and scope
    - `python -m pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Conftest files are DRY with clear scope separation</done>
</task>

<task type="auto">
  <name>Task 3: Register pytest markers in tests/pytest.ini</name>
  <files>tests/pytest.ini</files>
  <read_first>
    - tests/pytest.ini (current configuration)
    - .planning/phases/07-test-coverage-validation/07-VALIDATION.md (marker definitions)
  </read_first>
  <action>
Update tests/pytest.ini to register all Phase 7 test markers. Add `[tool:pytest]` section (or update if exists) with:

```ini
[pytest]
markers =
    docker: marks tests that require Docker container (skip with -m "not docker")
    slow: marks tests as slow (concurrency, E2E, stress tests)
    crud: marks Core CRUD workflow tests
    approval: marks approval workflow tests (approve + reject)
    revert: marks version revert tests
    admin: marks admin panel tests
    stress: marks stress tests (wide CSV, large row count)
    security: marks security/attack tests
    flaky: marks known flaky tests (retry once)

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --strict-markers
```

Include all markers from VALIDATION.md. This prevents "unknown marker" warnings when tests use `@pytest.mark.docker` etc.

After updating, run `python -m pytest --markers` to verify all markers are registered.
  </action>
  <verify>
    <automated>python -m pytest --markers 2>&1 | grep -E "docker|slow|crud|approval|revert|admin|stress|security|flaky"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest --markers` lists all Phase 7 markers (docker, slow, crud, approval, revert, admin, stress, security, flaky)
    - tests/pytest.ini contains `addopts = --strict-markers`
    - tests/pytest.ini contains `testpaths = tests`
  </acceptance_criteria>
  <done>pytest.ini has all Phase 7 markers registered and enforced</done>
</task>

<task type="auto">
  <name>Task 4: Update requirements-dev.txt with Phase 7 testing tools</name>
  <files>requirements-dev.txt</files>
  <read_first>
    - requirements-dev.txt (current dependencies)
    - .planning/phases/07-test-coverage-validation/07-RESEARCH.md section "Standard Stack" (tool versions)
  </read_first>
  <action>
Ensure requirements-dev.txt includes all Phase 7 testing tools with pinned versions. Add or update:

```
pytest==8.1.1
pytest-cov==5.0.0
freezegun==1.5.1
hypothesis==6.90.0
pytest-timeout==2.1.0
playwright==1.40.0
pytest-playwright>=0.4.0
```

Verification:
- `pip install -r requirements-dev.txt` installs without errors
- `python -c "import pytest; import playwright; import hypothesis; print('OK')"` confirms imports work
- After install, run `playwright install chromium` to set up browser binaries (note: this is manual after pip install)

Document in PLAN.md summary: "Executor must run `playwright install chromium` after installing requirements-dev.txt to download browser binaries for E2E tests."
  </action>
  <verify>
    <automated>grep -E "pytest==|pytest-cov==|freezegun==|hypothesis==|pytest-timeout==|playwright==|pytest-playwright" requirements-dev.txt</automated>
  </verify>
  <acceptance_criteria>
    - requirements-dev.txt contains pytest==8.1.1
    - requirements-dev.txt contains pytest-cov==5.0.0
    - requirements-dev.txt contains freezegun==1.5.1
    - requirements-dev.txt contains hypothesis==6.90.0
    - requirements-dev.txt contains pytest-timeout==2.1.0
    - requirements-dev.txt contains playwright==1.40.0
    - requirements-dev.txt contains pytest-playwright>=0.4.0
  </acceptance_criteria>
  <done>requirements-dev.txt has all Phase 7 testing tools with locked versions</done>
</task>

<task type="auto">
  <name>Task 5: Verify all unit tests pass with ≥80% coverage</name>
  <files>tests/unit/</files>
  <read_first>
    - tests/unit/conftest.py (fixture setup)
    - tests/unit/test_*.py (all unit test files)
  </read_first>
  <action>
Run full unit test suite with coverage reporting:

```bash
python -m pytest tests/unit/ -q --cov=bin/ --cov-report=term-missing --cov-report=html
```

Expected output:
- All tests pass (N passed, 0 failed)
- Coverage report shows ≥80% for each backend module:
  - bin/wl_constants.py: 100%+
  - bin/wl_logging.py: 100%+
  - bin/wl_validation.py: ≥95%
  - bin/wl_ratelimit.py: ≥86%
  - bin/wl_rbac.py: ≥99%
  - bin/wl_presence.py: 100%+
  - bin/wl_csv.py: ≥95%
  - bin/wl_rules.py: ≥90%
  - bin/wl_trash.py: ≥90%
  - bin/wl_versions.py: ≥90%
  - bin/wl_audit.py: ≥85%
  - bin/wl_filelock.py: ≥90%
  - bin/wl_limits.py: ≥80% (after Task 1 fix)
  - bin/wl_approval.py: ≥90%
  - bin/wl_notify.py: ≥85%
  - bin/wl_replay.py: ≥85%

If any module falls below 80%, identify missing tests and add unit test cases (but do NOT extend into new integration tests — stay in Phase 1 scope).

Generate HTML coverage report: `htmlcov/index.html` for later Phase 8 PUBL-05 documentation.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/ -q --cov=bin/ --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/ -q --cov=bin/` exits 0 (all tests pass)
    - Coverage report shows all backend modules at ≥80%
    - `htmlcov/index.html` exists with coverage summary
    - No failures or errors in unit test output
  </acceptance_criteria>
  <done>Unit test suite passes with ≥80% coverage across all backend modules</done>
</task>

</tasks>

<verification>
After Task 5, run the following to verify Plan 07-01 is complete:

1. **Test execution:**
   ```bash
   python -m pytest tests/unit/ -q --cov=bin/ --tb=short
   ```
   Expected: All tests pass, ≥80% coverage

2. **Conftest consolidation:**
   ```bash
   grep -n "def " tests/conftest.py tests/unit/conftest.py | sort
   ```
   Expected: No duplicate fixture names

3. **Marker registration:**
   ```bash
   python -m pytest --markers | grep -E "docker|slow|crud|approval"
   ```
   Expected: All Phase 7 markers listed

4. **Requirements check:**
   ```bash
   grep -E "pytest|playwright|hypothesis" requirements-dev.txt
   ```
   Expected: All tools present with versions

5. **Baseline established:**
   - 389+ unit tests passing
   - ≥80% coverage on all 16 backend modules
   - Conftest is DRY with clear scope separation
   - pytest.ini has all markers registered
</verification>

<success_criteria>
Plan 07-01 is complete when:
- [ ] Task 1: test_limits.py has 0 failures
- [ ] Task 2: conftest files are DRY with no duplication
- [ ] Task 3: pytest.ini has all Phase 7 markers registered
- [ ] Task 4: requirements-dev.txt has all testing tools with versions
- [ ] Task 5: `python -m pytest tests/unit/ -q --cov=bin/` exits 0 with ≥80% coverage
- [ ] HTML coverage report generated at htmlcov/index.html
- [ ] No regressions in any module
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-coverage-validation/07-01-SUMMARY.md` with:
- Test results (N tests, N coverage %)
- Fixed test_limits.py: before/after
- Conftest consolidation summary
- pytest.ini markers registered
- requirements-dev.txt tools added
- baseline coverage metrics per module
</output>
