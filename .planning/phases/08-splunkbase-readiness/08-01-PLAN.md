---
phase: 08-splunkbase-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate.sh
  - scripts/verify_appinspect.sh
  - default/app.conf
  - default/app.manifest
  - docs/APPINSPECT_NOTES.md
  - Makefile
autonomous: true
requirements:
  - PUBL-01
must_haves:
  truths:
    - "AppInspect standard tag set runs with zero hard failures"
    - "AppInspect cloud tag set runs and identifies cloud-specific issues"
    - "All high/critical issues are either fixed or documented with justification"
    - "package.sh produces .spl file with clean validation output"
    - "App can be packaged and validated via make appinspect"
  artifacts:
    - path: scripts/verify_appinspect.sh
      provides: AppInspect CLI wrapper (standard + cloud tag sets)
      exports: ["--standard", "--cloud"]
    - path: docs/APPINSPECT_NOTES.md
      provides: Issue log with justifications for unfixable warnings
      min_lines: 20
    - path: Makefile
      provides: make appinspect target for one-command validation
      contains: "appinspect:"
  key_links:
    - from: package.sh
      to: verify_appinspect.sh
      via: validation pipeline
      pattern: "verify_appinspect"
    - from: validate.sh
      to: app.conf
      via: build number verification
      pattern: "build ="
    - from: app.manifest
      to: app.conf
      via: version matching
      pattern: "version"
---

<objective>
Run AppInspect validation (both standard and cloud tag sets) against the v3.0 rewrite, identify all issues, fix what's fixable, document remaining items, and establish a Makefile target for future validation.

Purpose: Demonstrate compliance with Splunk's publishing requirements and cloud compatibility. Identify Python/JS patterns that violate AppInspect rules and document exceptions.

Output: Clean AppInspect report, documented exceptions in APPINSPECT_NOTES.md, validate.sh pre-flight checks updated with findings, Makefile appinspect target
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-splunkbase-readiness/08-CONTEXT.md
@.planning/phases/08-splunkbase-readiness/08-RESEARCH.md

## Canonical References

- `default/app.conf` — App metadata, version, build number
- `app.manifest` — Splunkbase manifest template (needs population)
- `scripts/package.sh` — .spl packaging script (enhancement target)
- `scripts/validate.sh` — Existing pre-flight checks
- `Makefile` — Build targets (add appinspect)
- Backend modules: `bin/wl_*.py` (14 modules, Python 3 compliance target)
- Frontend modules: `appserver/static/modules/wl_*.js` (15+ AMD modules, JS security audit target)
- Conf files: `default/*.conf` (8+ files, Splunk spec validation target)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify_appinspect.sh wrapper script</name>
  <files>scripts/verify_appinspect.sh</files>
  <read_first>
    - scripts/package.sh (existing packaging patterns)
    - default/app.conf (version reference)
    - Makefile (existing build patterns)
  </read_first>
  <action>
Create a new bash script `scripts/verify_appinspect.sh` that:

1. **Accepts command-line flags**:
   - `--standard` (default): Run AppInspect with standard tag set
   - `--cloud`: Run AppInspect with cloud tag set (additional check)
   - `--both`: Run both standard and cloud (exhaustive check)

2. **Workflow**:
   - Call package.sh to produce dist/*.spl
   - Run splunk-appinspect with appropriate tag sets
   - Capture output to temporary file
   - Parse output for: high issues, critical issues, warning count
   - Report summary: "X high, Y critical, Z warnings"
   - Exit 0 if high+critical == 0, exit 1 otherwise

3. **Cloud tag set specifics**:
   - Use --included-tags cloud flag to AppInspect
   - This will flag patterns like: localhost:8089 direct HTTP, disabled dashboard caching, deprecated directives

4. **Output format**:
   === AppInspect Validation (standard) ===
   File: dist/wl_manager-1.0.0.spl
   High: 0, Critical: 0, Warnings: 5
   Status: PASS

5. **Error handling**:
   - If splunk-appinspect not installed, print helpful message directing to pip install
   - If package.sh fails, propagate error

6. **Shell robustness**:
   - Use set -e for fail-on-error
   - Trap ERR for cleanup
   - Quote all variables
  </action>
  <verify>
    - Script exists at scripts/verify_appinspect.sh
    - Script is executable via test -x
    - Script accepts flags and produces output mentioning High/Critical/Warnings
  </verify>
  <done>Script created and tested. Accepts standard/cloud/both flags. Produces summary output with pass/fail status.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Python modules for AppInspect compliance</name>
  <files>bin/wl_handler.py, bin/wl_*.py, docs/APPINSPECT_NOTES.md</files>
  <read_first>
    - bin/wl_constants.py (pattern reference)
    - bin/wl_audit.py (cloud compat concern: localhost:8089 urllib usage)
    - bin/wl_rbac.py
    - bin/wl_validation.py
    - bin/wl_limits.py
    - bin/wl_approval.py
    - bin/wl_csv.py
    - bin/wl_versions.py
    - bin/wl_rules.py
    - bin/wl_trash.py
    - bin/wl_logging.py
    - bin/wl_ratelimit.py
    - bin/wl_presence.py
    - bin/wl_filelock.py
    - bin/wl_replay.py
    - bin/wl_notify.py
  </read_first>
  <action>
Systematically audit all 14 Python backend modules for AppInspect violations:

1. **Bare except clauses** (AppInspect flag: avoid bare except):
   - Search all files for "except:" without exception type
   - Fix: Replace with specific exception types or "except Exception as e:"
   - Expected: 0 bare except found after fixes

2. **Dangerous patterns** (AppInspect: no eval, exec, or dynamic code construction):
   - Verify no eval() calls in production code (not acceptable for user input)
   - Verify no exec() calls in production code
   - Verify no __import__() for dynamic imports
   - Verify no compile() for untrusted input
   - Expected: 0 dangerous patterns found

3. **Import patterns** (Splunk SDK lazy loading):
   - Confirm all modules import Splunk SDK inside functions, not at module level (for offline test support)
   - Exception: sys.path.insert() for inter-module imports OK (Splunk bin/ limitation)
   - Expected: All modules follow lazy import pattern documented in code comments

4. **Logging compliance** (AppInspect: use Splunk SDK logging):
   - Confirm all modules import from wl_logging.py
   - Verify no direct logging.getLogger() at module level
   - Verify no print() statements in production code
   - Expected: 0 direct print() or logging.getLogger() calls

5. **File path security** (AppInspect: no hardcoded paths):
   - Confirm paths built via make_splunk_path() or safe_realpath() from wl_validation.py
   - Avoid /opt/splunk/ hardcoded; use SPLUNK_HOME + build_csv_path()
   - Expected: All paths validated through wl_validation module

6. **Custom commands compliance** (wl_expiration_cleanup.py, wl_expiring_soon.py):
   - Confirm proper SearchCommand class inheritance
   - Confirm no bare except, no direct print()
   - Confirm uses wl_logging.get_audit_logger()

7. **Python 3 syntax**:
   - Verify no Python 2 print statements without parentheses
   - Verify no old-style exception syntax
   - Confirm all string encoding explicit and UTF-8 compliant

Create APPINSPECT_NOTES.md documenting:
  - Issues found and how they were fixed
  - Any remaining issues with explicit justifications
  - List of modules audited: 14 total
  - Audit date and pass status

  </action>
  <verify>
    - No bare except clauses found in any module
    - No print() statements in production code
    - All modules use wl_logging for logging
    - No module-level Splunk SDK imports (except via sys.path.insert)
    - docs/APPINSPECT_NOTES.md exists with 14 modules listed
  </verify>
  <done>All 14 Python modules audited. AppInspect compliance issues fixed or documented in APPINSPECT_NOTES.md. No bare except, no print(), no direct Splunk SDK imports at module level.</done>
</task>

<task type="auto">
  <name>Task 3: Audit JavaScript modules for AppInspect security patterns</name>
  <files>appserver/static/whitelist_manager.js, appserver/static/control_panel.js, appserver/static/modules/*.js, appserver/static/notifications.js, docs/APPINSPECT_NOTES.md</files>
  <read_first>
    - appserver/static/whitelist_manager.js (main entry point)
    - appserver/static/control_panel.js (admin panel entry point)
    - appserver/static/modules/wl_rest.js (REST helper, high exposure)
    - appserver/static/modules/wl_table.js (table operations)
    - appserver/static/modules/wl_state.js (state manager)
  </read_first>
  <action>
Audit all 15+ JavaScript AMD modules for AppInspect security red flags:

1. **Unsafe HTML assignment** (AppInspect flag: innerHTML with unsanitized content):
   - Search for innerHTML or .html() method calls that assign data
   - Fix approach: Confirm data source is trusted (Splunk templates, not REST responses)
   - User-supplied values should use .text() or equivalent text-only assignment
   - Expected: No innerHTML assignments to user-supplied data

2. **DOM write safety** (AppInspect: textContent vs HTML assignment):
   - Pattern: User-supplied values must use text-only assignment methods
   - Review context: If variable comes from REST response, confirm backend sanitization
   - Expected: No unsanitized user input written via HTML methods

3. **Inline event handlers** (AppInspect: prefer addEventListener):
   - Search for onclick, onload, onchange attributes
   - Fix: Replace with jQuery .on() or addEventListener()
   - Expected: 0 inline event handlers

4. **Direct window/global manipulation**:
   - Search for dynamic property access patterns
   - Confirm AMD modules don't pollute global namespace
   - Confirm all modules use proper define() pattern
   - Expected: All modules follow AMD pattern correctly

5. **Splunk SDK compliance**:
   - Confirm jQuery and mvc/utils used for REST calls
   - Confirm all AJAX calls have error handlers
   - Confirm proper use of mvc/utils.getPageInfo() for session info
   - Expected: All REST calls properly wrapped

6. **Security documentation**:
   - Add comments for security-relevant sections: CSRF handling, authorization, audit logging
   - Expected: Key security sections have clear comments

Append to APPINSPECT_NOTES.md:
  - JavaScript files audited: 15+ AMD modules
  - Security issues found and status
  - HTML assignment patterns confirmed safe
  - DOM write patterns confirmed sanitized
  </action>
  <verify>
    - No eval() calls in any JS file
    - No inline onclick/onload event handlers
    - All JS files in appserver/static/modules/ follow AMD define() pattern
    - APPINSPECT_NOTES.md contains JS audit section with 15+ modules listed
  </verify>
  <done>All JavaScript modules audited for AppInspect security patterns. No unsafe HTML assignments, no inline event handlers, all AJAX calls use jQuery with error handlers. Security findings documented in APPINSPECT_NOTES.md.</done>
</task>

<task type="auto">
  <name>Task 4: Audit conf files against Splunk spec</name>
  <files>default/app.conf, default/restmap.conf, default/authorize.conf, default/indexes.conf, default/savedsearches.conf, default/props.conf, default/transforms.conf, default/commands.conf, default/web.conf, default/default.meta, metadata/default.meta, docs/APPINSPECT_NOTES.md</files>
  <read_first>
    - default/app.conf
    - default/restmap.conf
    - default/authorize.conf
    - default/indexes.conf
    - default/savedsearches.conf
    - default/props.conf
    - default/transforms.conf
    - default/commands.conf
  </read_first>
  <action>
Audit all configuration files for AppInspect compliance:

1. **app.conf**:
   - Confirm version = 1.0.0 (Splunkbase publication)
   - Confirm build = {build_number} matches app.manifest
   - Confirm author, description, check_for_updates set
   - Confirm no deprecated directives removed in Splunk 7.0+

2. **restmap.conf**:
   - Confirm /custom/wl_manager endpoint mapped correctly
   - Confirm output format valid (application/json)
   - Confirm streaming disabled appropriately

3. **authorize.conf**:
   - Confirm role definitions: wl_viewer, wl_editor
   - Confirm capabilities correctly assigned
   - Check: No deprecated capabilities

4. **indexes.conf**:
   - Confirm wl_audit index defined with datatype=event, disabled=0
   - Confirm no deprecated directives

5. **savedsearches.conf**:
   - Confirm all searches have unique names
   - Confirm scheduled searches use valid cron syntax
   - Confirm alert actions properly configured

6. **props.conf**:
   - Confirm TRUNCATE = 0 for wl_audit (handles long audit events)
   - Confirm TRANSFORMS assignments valid

7. **transforms.conf**:
   - Confirm all lookups defined with correct file references
   - Verify CSV files exist (rule_csv_map.csv, DR*.csv)

8. **commands.conf**:
   - Confirm wl_expiration_cleanup and wl_expiring_soon commands defined
   - Confirm Python path correct
   - Confirm Takes/returns clauses present

9. **web.conf**:
   - Confirm static file serving config correct
   - Confirm no deprecated directives

10. **metadata/default.meta**:
    - Confirm exports for views, lookups, saved searches
    - Confirm RBAC via access restrictions

Append to APPINSPECT_NOTES.md:
  - Conf files audited: 8+ files
  - Issues found and fixes applied
  - All files conform to Splunk 9.x schema

  </action>
  <verify>
    - app.conf contains "version = 1.0.0"
    - app.conf contains a build number
    - authorize.conf contains role definitions (wl_viewer, wl_editor)
    - indexes.conf contains wl_audit index definition
    - All .conf files exist and can be parsed
    - APPINSPECT_NOTES.md documents all conf files audited
  </verify>
  <done>All 8+ conf files audited against Splunk schema. No deprecated directives found. All configuration valid for Splunk 9.x. Issues documented in APPINSPECT_NOTES.md.</done>
</task>

<task type="auto">
  <name>Task 5: Update validate.sh with AppInspect findings and create Makefile target</name>
  <files>scripts/validate.sh, Makefile</files>
  <read_first>
    - scripts/validate.sh (existing validation checks)
    - scripts/verify_appinspect.sh (just created)
    - Makefile (existing targets)
  </read_first>
  <action>
1. **Enhance validate.sh**:
   - Add section for AppInspect Pre-flight Checks
   - Add grep-based checks discovered from AppInspect findings
   - Check for: bare except, print statements, dynamic pattern violations
   - Check for: module-level Splunk SDK imports, unlogged print statements
   - Ensure validate.sh still passes with existing checks

2. **Create Makefile target**:
   - Add target: appinspect
   - Dependencies: validate (run pre-flight checks first)
   - Commands: Call verify_appinspect.sh with --both flag
   - Also add target: validate (wrapper for scripts/validate.sh)
   - Add .PHONY declarations for both targets
   - Document in comments: "make appinspect — Run full AppInspect + package validation"

  </action>
  <verify>
    - scripts/validate.sh contains new AppInspect Pre-flight Checks section
    - scripts/validate.sh executes without errors (returns 0 on success)
    - Makefile contains target appinspect: with dependency on validate
    - Makefile contains target validate: with scripts/validate.sh call
    - make appinspect executes successfully
  </verify>
  <done>validate.sh enhanced with AppInspect pre-flight checks. Makefile updated with appinspect and validate targets. Full validation pipeline ready.</done>
</task>

<task type="auto">
  <name>Task 6: Complete app.manifest with Splunkbase metadata</name>
  <files>default/app.manifest, default/app.conf</files>
  <read_first>
    - default/app.manifest (existing template)
    - default/app.conf (metadata source)
  </read_first>
  <action>
Update app.manifest with complete Splunkbase information:

Fields to populate:
- author: RelativisticJet (GitHub username from CLAUDE.md)
- version: 1.0.0 (match app.conf)
- releaseDate: 2026-04-02 (today's date in YYYY-MM-DD format)
- description: "Splunk app for managing detection rule whitelists with full audit trail. Enables SOC analysts to safely edit CSV lookup whitelists with change tracking, versioning, and role-based approval workflows."
- license: MIT (match LICENSE file)
- licenseUrl: https://github.com/RelativisticJet/wl_manager/blob/main/LICENSE
- homepageUrl: https://github.com/RelativisticJet/wl_manager
- support_level: community
- minimum_splunk_version: 9.0.0
- maximum_splunk_version: (leave unset)
- disable_from_UI: false
- state: published

Verify JSON is valid and version matches app.conf.

  </action>
  <verify>
    - default/app.manifest contains author field set to RelativisticJet
    - default/app.manifest contains version field set to 1.0.0
    - default/app.manifest contains releaseDate field set to 2026-04-02
    - default/app.manifest contains license field set to MIT
    - default/app.manifest parses as valid JSON
    - Version in app.manifest matches app.conf version
  </verify>
  <done>app.manifest populated with all required Splunkbase metadata. JSON is valid. Version and author match CLAUDE.md and app.conf.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run verify_appinspect.sh with standard flag and confirm Status: PASS in output
2. Run verify_appinspect.sh with cloud flag and review cloud tag findings
3. Confirm APPINSPECT_NOTES.md exists with 14 Python modules, 15+ JS modules, and 8+ conf files documented
4. Confirm make appinspect executes without errors
5. Confirm make validate passes all pre-flight checks
6. Confirm app.manifest is valid JSON and version matches app.conf
</verification>

<success_criteria>
Plan 08-01 is complete when:
- AppInspect validation passes standard tag set (0 high/critical issues)
- AppInspect cloud tag set runs and issues logged in APPINSPECT_NOTES.md
- Python modules audited: 14 files, no bare except, no print(), all use wl_logging
- JavaScript modules audited: 15+ files, no unsafe HTML assignments, all AJAX wrapped
- Conf files audited: 8+ files, all valid per Splunk 9.x schema
- validate.sh enhanced with AppInspect pre-flight checks
- Makefile has appinspect and validate targets
- app.manifest populated with version 1.0.0, author, releaseDate, license, description
- All changes committed to git

Requirement PUBL-01 partially satisfied: AppInspect compliance established, packaging ready for validation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-splunkbase-readiness/08-01-SUMMARY.md` documenting:
- AppInspect standard validation result (pass/fail + issue count)
- AppInspect cloud validation result (issues found)
- Python audit findings (modules, issues fixed)
- JavaScript audit findings (modules, security patterns)
- Conf audit findings (files, deprecated directives removed)
- Makefile targets added
- app.manifest completion status
</output>
