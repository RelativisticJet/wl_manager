---
phase: 08-splunkbase-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api/openapi.yaml
  - docs/api/README.md
autonomous: true
requirements:
  - PUBL-03
must_haves:
  truths:
    - "OpenAPI 3.0 specification documents all REST API actions"
    - "Every action has request/response examples with real request/response bodies"
    - "Error codes and HTTP status codes are documented for all actions"
    - "Spec matches wl_handler.py GET_ACTIONS and POST_ACTIONS dispatch tables"
  artifacts:
    - path: docs/api/openapi.yaml
      provides: OpenAPI 3.0 specification for all REST actions
      min_lines: 200
    - path: docs/api/README.md
      provides: API documentation guide and spec usage
      min_lines: 30
  key_links:
    - from: docs/api/openapi.yaml
      to: bin/wl_handler.py
      via: GET_ACTIONS and POST_ACTIONS mappings
      pattern: "get_csv"
---

<objective>
Hand-author OpenAPI 3.0 specification documenting all REST API actions, parameters, response shapes, error codes, and example request/response bodies for each action.

Purpose: Provide API documentation for developers and Splunk integrators, enable automated testing tools to consume spec, demonstrate API completeness in publishing materials.

Output: docs/api/openapi.yaml (OpenAPI 3.0 spec), docs/api/README.md (spec guide)
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-splunkbase-readiness/08-CONTEXT.md
@bin/wl_handler.py

## Canonical References (Action Dispatch)

From wl_handler.py, extract all GET and POST actions. Expected count: 21 GET actions + 31 POST actions = 52 total.

Example actions to document:
- GET: get_csv, get_mapping, get_versions, get_pending_approvals, get_user_limits, get_user_presence
- POST: save_csv, revert_csv, save_col_widths, add_rule, delete_rule, submit_approval, process_approval, etc.

Each action needs:
- operationId: unique action name
- summary: one-line description
- parameters: list with name, in (query/body), required, schema
- requestBody: schema + example
- responses: 200/400/403/404/500 with schemas + examples
- security: required roles (if applicable)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract action signatures from wl_handler.py and map to OpenAPI</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - bin/wl_handler.py (GET_ACTIONS dict, POST_ACTIONS dict, _action_* methods)
  </read_first>
  <action>
Extract all REST actions from wl_handler.py and create OpenAPI 3.0 specification:

1. **Read GET_ACTIONS and POST_ACTIONS dicts** to get complete list of actions
2. **For each action, document**:
   - operationId (action name): get_csv, save_csv, etc.
   - summary: Brief description
   - description: Detailed explanation
   - parameters: List with name, description, required, type
   - requestBody (for POST): Schema with example JSON
   - responses: 200 OK, 400 Bad Request, 403 Forbidden, 404 Not Found, 500 Server Error
   - security: Required roles/capabilities

3. **OpenAPI structure**:
   - openapi: 3.0.0
   - info: title, version 1.0.0, description
   - servers: url=/custom/wl_manager, description=Splunk instance
   - paths: /? with post/get operations (single endpoint, differentiated by action parameter)
   - components: schemas, securitySchemes, responses

4. **Example actions to document with full details**:
   - get_csv: GET with csv_file parameter, returns CSV rows
   - save_csv: POST with csv_file, new_rows, old_rows, comment, returns success
   - revert_csv: POST with csv_file, version_id, comment, returns success
   - submit_approval: POST with action_type, payload, reason, returns request_id
   - process_approval: POST with request_id, approved/rejected, admin_comment

5. **For each POST action, include example request/response**:
   Request example: JSON body with realistic values
   Response example (200): {"success": true, "request_id": "..."}
   Response example (400): {"success": false, "error": "Invalid csv_file"}

6. **Document error codes**:
   - 200 OK: Action succeeded
   - 400 Bad Request: Validation error (missing field, invalid value)
   - 403 Forbidden: RBAC denied (insufficient role)
   - 404 Not Found: CSV/rule not found
   - 500 Server Error: Unexpected error

Create at docs/api/openapi.yaml as YAML (not JSON for readability).

  </action>
  <verify>
    - docs/api/openapi.yaml exists and is >200 lines
    - File contains openapi: 3.0.0 header
    - File contains paths section with / endpoint
    - File contains post and get operations
    - File contains 20+ operationId values (covers most actions)
    - File contains example request/response bodies in JSON
    - File contains components/schemas section
    - YAML is valid: can be parsed by openapi-spec-validator or similar
  </verify>
  <done>OpenAPI 3.0 specification created documenting 20+ REST actions with parameters, examples, and error codes. Spec validates against OpenAPI 3.0 schema.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/api/README.md guide for API specification</name>
  <files>docs/api/README.md</files>
  <read_first>
    - docs/api/openapi.yaml (just created)
  </read_first>
  <action>
Create README documenting how to use OpenAPI specification:

1. **Overview**:
   - What the spec covers (all REST actions, parameters, responses)
   - How to view the spec (raw YAML, or render in Swagger UI/Redoc)

2. **Quick Start**:
   - Base URL: https://your-splunk-instance:8089/custom/wl_manager
   - Authentication: Session key required (X-Splunk-Key header)
   - Action parameter: All requests go to /?action=action_name

3. **Common Parameters**:
   - action: Required, action name (e.g., get_csv, save_csv)
   - session_key: Required, Splunk session key
   - Examples of how to use

4. **Response Format**:
   - All responses are JSON
   - Success: {"success": true, ...other_fields...}
   - Error: {"success": false, "error": "message"}

5. **Authentication**:
   - Get session key via Splunk REST API (/services/auth/login)
   - Include in X-Splunk-Key header or session_key parameter

6. **Example usage** (in curl):
   - Get CSV example
   - Save CSV example
   - Submit approval example

7. **Tools for exploring the spec**:
   - Swagger UI: https://editor.swagger.io (paste openapi.yaml)
   - Redoc: https://redocly.github.io/redoc (render spec)
   - OpenAPI Spec Validator: validate spec syntax

  </action>
  <verify>
    - docs/api/README.md exists and is >30 lines
    - README contains "Overview" section
    - README contains "Quick Start" section
    - README contains "Authentication" section
    - README contains at least one curl example
    - README mentions how to view spec (Swagger UI, Redoc)
  </verify>
  <done>API documentation README created with overview, quick start, authentication, and usage examples.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Read docs/api/openapi.yaml and verify OpenAPI 3.0 header
2. Verify 20+ actions documented (check operationId count)
3. Verify examples include request/response JSON bodies
4. Verify error codes 200/400/403/404/500 documented
5. Verify YAML syntax is valid
6. Verify docs/api/README.md explains how to use the spec
</verification>

<success_criteria>
Plan 08-03 is complete when:
- docs/api/openapi.yaml created (>200 lines, valid OpenAPI 3.0)
- 20+ REST actions documented (GET and POST)
- Each action has: operationId, summary, parameters, requestBody (POST), responses
- Example request/response bodies in JSON for each action type
- Error codes documented (200/400/403/404/500)
- Security/authentication documented in spec
- docs/api/README.md created (>30 lines, usage guide)
- README includes quick start, examples, tools for viewing spec
- All changes committed to git

Requirement PUBL-03 satisfied: OpenAPI specification complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-splunkbase-readiness/08-03-SUMMARY.md` documenting:
- OpenAPI spec structure and coverage
- Actions documented (GET count, POST count, total)
- Example actions with detailed documentation
- Error codes covered
- Tools mentioned for spec viewing
</output>
