---
phase: 08-splunkbase-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/metrics_collector.py
  - CODE_METRICS.md
  - docs/CODE_METRICS.md
  - Makefile
autonomous: true
requirements:
  - PUBL-05
must_haves:
  truths:
    - "Python modules: all have CC <15, functions <100 lines, coverage >=80%"
    - "JavaScript modules: cyclomatic complexity measured and reported"
    - "Metrics script enforces quality thresholds and fails on violations"
    - "Metrics report published to both root and docs/ for visibility"
  artifacts:
    - path: scripts/metrics_collector.py
      provides: Metrics collection script (radon + escomplex)
      exports: ["--gate", "--report"]
    - path: CODE_METRICS.md
      provides: Metrics summary at root for GitHub visibility
      min_lines: 30
    - path: docs/CODE_METRICS.md
      provides: Full metrics report in .spl package
      min_lines: 50
  key_links:
    - from: scripts/metrics_collector.py
      to: bin/wl_*.py
      via: radon analysis
      pattern: "cc"
    - from: Makefile
      to: scripts/metrics_collector.py
      via: make metrics target
      pattern: "metrics:"
---

<objective>
Create metrics collection script to measure Python cyclomatic complexity (radon), JavaScript complexity (escomplex), function sizes, and test coverage. Publish metrics to CODE_METRICS.md and enforce quality thresholds.

Purpose: Demonstrate code quality improvements from v2.0 to v3.0 (modular rewrite), satisfy PUBL-05 requirement, provide CI/CD gate for future changes.

Output: metrics_collector.py script, CODE_METRICS.md (root + docs), make metrics target
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-splunkbase-readiness/08-CONTEXT.md

## Quality Thresholds (from REQUIREMENTS.md)

- **Cyclomatic Complexity**: CC <15 for all functions, average B+ for all modules
- **Function Size**: <100 lines per function (checked via line count)
- **Test Coverage**: >=80% for all modules (from pytest-cov)
- **Module Size**: <1000 lines per module (size check)

## Metrics Scope

Python:
- 14 modules in bin/ (wl_*.py)
- 2 custom commands: wl_expiration_cleanup.py, wl_expiring_soon.py

JavaScript:
- 15+ AMD modules in appserver/static/modules/
- 2 entry points: whitelist_manager.js, control_panel.js

Coverage:
- From Phase 7 pytest runs (htmlcov/ directory)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/metrics_collector.py with radon and escomplex integration</name>
  <files>scripts/metrics_collector.py</files>
  <read_first>
    - bin/wl_constants.py (sample module to measure)
  </read_first>
  <action>
Create Python script that collects and reports code metrics:

1. **Python metrics via radon** (installed in requirements-dev.txt):
   - Cyclomatic complexity: radon cc bin/ -a --order SCORE
   - Raw metrics: radon raw bin/ -s (function count, lines)
   - Collect per-module CC scores and avg grade (A/B/C/D/F)

2. **JavaScript metrics via escomplex** (npm install escomplex):
   - Measure cyclomatic complexity for appserver/static/**/*.js
   - Report per-file complexity

3. **Test coverage from pytest-cov**:
   - Read htmlcov/status.json or .coverage file
   - Extract overall coverage percentage
   - Extract per-module coverage

4. **Script logic**:
   - Argument: --gate (enforce thresholds, exit 1 on violation) or --report (generate report only)
   - Threshold enforcement:
     * No function >100 lines (check radon raw output)
     * No module >1000 lines (check radon raw output)
     * All modules CC <15 (check radon cc output)
     * Overall coverage >=80% (check pytest-cov)
   - Generate metrics report with:
     * Python section: per-module CC, avg grade, coverage
     * JavaScript section: per-file complexity
     * Summary: pass/fail status, violations listed

5. **Output format**:
   Standard run: Print summary to stdout
   --report flag: Write to CODE_METRICS.md

  </action>
  <verify>
    - scripts/metrics_collector.py exists and is executable
    - Script runs without errors: python scripts/metrics_collector.py --report
    - Script generates output mentioning radon, coverage, complexity
    - Script accepts --gate and --report flags
    - Script exits 0 on pass, 1 on failure (when using --gate)
  </verify>
  <done>Metrics collector script created. Runs radon for Python complexity, escomplex for JS, pytest-cov for coverage. Enforces thresholds with --gate flag.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and publish CODE_METRICS.md to root and docs/</name>
  <files>CODE_METRICS.md, docs/CODE_METRICS.md</files>
  <read_first>
    - scripts/metrics_collector.py (just created)
  </read_first>
  <action>
Run metrics_collector.py --report and create metrics reports:

1. **Collect metrics**:
   - Run: python scripts/metrics_collector.py --report
   - Capture output

2. **Create CODE_METRICS.md at root** (GitHub-visible):
   - Title: "Code Quality Metrics — v3.0 Modular Rewrite"
   - Executive summary (1-3 sentences): "v3.0 achieves modularization with C <15 all modules, 80%+ coverage"
   - Python metrics table: Module | Lines | Functions | CC Avg | Grade | Coverage
   - JavaScript metrics table: Module | Complexity | Grade
   - Test coverage summary: Overall coverage %, per-module breakdown
   - Passing/failing criteria checklist

3. **Create identical docs/CODE_METRICS.md** (in .spl package):
   - Same content as root version
   - Serves as documentation for end-users

  </action>
  <verify>
    - CODE_METRICS.md exists at root with >30 lines
    - docs/CODE_METRICS.md exists with >50 lines
    - Both files contain metrics tables (Python + JS)
    - Both files reference overall coverage percentage (should be >=80%)
    - Both files show per-module grades
  </verify>
  <done>Metrics reports generated and published to CODE_METRICS.md (root) and docs/CODE_METRICS.md. Both contain Python/JS metrics, coverage summary, and pass/fail status.</done>
</task>

<task type="auto">
  <name>Task 3: Add make metrics target to Makefile</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (existing targets)
    - scripts/metrics_collector.py (just created)
  </read_first>
  <action>
Add Makefile target for running metrics with quality gate:

1. **Add target: make metrics**:
   - Calls: python scripts/metrics_collector.py --gate
   - If fails (exit 1), stop build
   - If passes (exit 0), generate report

2. **Add target: make metrics-report**:
   - Calls: python scripts/metrics_collector.py --report
   - Always generates report (no exit code check)

3. **Update existing target: make appinspect** (from 08-01):
   - After appinspect validation, run metrics --gate
   - Full pre-release validation: appinspect + metrics

4. **Add .PHONY declarations**:
   - .PHONY: metrics metrics-report

  </action>
  <verify>
    - Makefile contains target "metrics:"
    - Makefile contains target "metrics-report:"
    - make metrics executes python scripts/metrics_collector.py --gate
    - make metrics returns 0 if thresholds pass, 1 if violated
    - make metrics-report generates report without failing
  </verify>
  <done>Makefile updated with make metrics and make metrics-report targets. Quality gate integrated into build pipeline.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run python scripts/metrics_collector.py --report and review output
2. Verify CODE_METRICS.md created at root with Python metrics table
3. Verify docs/CODE_METRICS.md created with same content
4. Verify make metrics executes without errors
5. Verify all Python modules have CC <15
6. Verify overall test coverage >=80%
</verification>

<success_criteria>
Plan 08-04 is complete when:
- scripts/metrics_collector.py created (radon + escomplex integration)
- Metrics script enforces thresholds: CC <15, functions <100 lines, coverage >=80%
- CODE_METRICS.md generated at root (>30 lines, GitHub-visible)
- docs/CODE_METRICS.md generated (>50 lines, in .spl package)
- Both metrics reports show per-module CC, grade, coverage
- JavaScript metrics included (complexity per module)
- make metrics target added to Makefile
- make metrics --gate enforces thresholds and fails on violations
- All changes committed to git

Requirement PUBL-05 satisfied: Code metrics documented and quality gates enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-splunkbase-readiness/08-04-SUMMARY.md` documenting:
- Metrics collection approach (radon, escomplex, pytest-cov)
- Quality thresholds enforced
- Python metrics results (module count, average CC, grade distribution)
- JavaScript metrics results (module count, complexity)
- Test coverage percentage
- Quality gate integration into Makefile
</output>
