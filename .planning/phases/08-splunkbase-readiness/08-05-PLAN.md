---
phase: 08-splunkbase-readiness
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - tests/integration/test_backward_compat_audit.py
  - tests/integration/test_backward_compat_versions.py
  - tests/integration/test_backward_compat_approval.py
  - scripts/test_upgrade_path.sh
  - docs/BACKWARD_COMPAT.md
autonomous: true
requirements:
  - PUBL-04
must_haves:
  truths:
    - "Pre-rewrite audit events parse correctly in audit.xml queries"
    - "Version manifests from v2.0 load and function in v3.0"
    - "Approval queue entries from v2.0 replay correctly in v3.0"
    - "Full upgrade path test passes: v2.0 -> v3.0 with data preservation"
  artifacts:
    - path: tests/integration/test_backward_compat_audit.py
      provides: Golden v2.0 audit event injection test
      min_lines: 50
    - path: tests/integration/test_backward_compat_versions.py
      provides: v2.0 version manifest fixture test
      min_lines: 40
    - path: tests/integration/test_backward_compat_approval.py
      provides: v2.0 approval queue replay test
      min_lines: 40
    - path: scripts/test_upgrade_path.sh
      provides: End-to-end Docker upgrade v2.0 to v3.0
      min_lines: 50
    - path: docs/BACKWARD_COMPAT.md
      provides: Backward compatibility test matrix and results
      min_lines: 50
  key_links:
    - from: test_backward_compat_audit.py
      to: default/data/ui/views/audit.xml
      via: SPL query verification
      pattern: "index=wl_audit"
    - from: test_upgrade_path.sh
      to: docker-compose.yml
      via: Container-based testing
      pattern: "wl_manager_test"
---

<objective>
Verify backward compatibility by testing pre-rewrite data (audit events, version manifests, approval queue) against v3.0 code. Run full upgrade path test (v2.0 to v3.0) in Docker to ensure data preservation.

Purpose: Demonstrate that existing customer data continues to work after upgrade. Provide confidence to Splunk admins considering upgrade.

Output: Backward compat tests (audit, versions, approval), upgrade path script, BACKWARD_COMPAT.md report
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-splunkbase-readiness/08-CONTEXT.md
@.planning/phases/08-splunkbase-readiness/08-VALIDATION.md

## Backward Compat Targets

From 08-CONTEXT.md:
- Audit events: Both static analysis (review SPL for field names) AND runtime testing (inject v2.0 events)
- Version manifests: Fixture-based test (create pre-rewrite manifest, feed through v3.0 code)
- Approval queue: Fixture-based test (create pre-rewrite queue entries, process through v3.0 wl_approval.py)
- Full upgrade path: Docker test (install v2.0, create data, upgrade to v3.0, verify all accessible)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_backward_compat_audit.py with golden v2.0 audit events</name>
  <files>tests/integration/test_backward_compat_audit.py, tests/fixtures/v2_audit_events.json</files>
  <read_first>
    - default/data/ui/views/audit.xml (SPL query validation targets)
    - bin/wl_audit.py (event structure reference)
  </read_first>
  <action>
Create integration test that injects pre-rewrite audit events and verifies they parse in audit.xml:

1. **Create test fixture**: tests/fixtures/v2_audit_events.json
   - Sample v2.0 audit events with action=added, removed, edited, revert
   - Include all expected fields: timestamp, analyst, detection_rule, csv_file, action, comment
   - Include removal fields: removed_row_count, value (numbered rows)
   - Include edit fields: edited_row_count, value (before/after)
   - Include revert fields: reverted_from_version, reverted_to_version, new_record_version

2. **Test cases**:
   - test_v2_added_event_parses: Inject added event, verify wl_audit index accepts it
   - test_v2_removal_event_parses: Inject removal event, verify fields present
   - test_v2_revert_event_parses: Inject revert event with *back fields, verify parsing
   - test_v2_event_field_names_match_queries: Verify audit.xml SPL queries find expected fields

3. **Test approach**:
   - Mock Splunk index write via patch decorator
   - Call wl_audit.post_audit_event() with v2.0 event structure
   - Verify no exceptions, event posted successfully

  </action>
  <verify>
    - tests/integration/test_backward_compat_audit.py exists with >50 lines
    - File contains test_v2_*_event_parses test cases (4+)
    - tests/fixtures/v2_audit_events.json contains sample v2.0 events
    - All test cases pass: pytest tests/integration/test_backward_compat_audit.py -v
  </verify>
  <done>Backward compatibility audit test created with golden v2.0 events. All event types (added, removed, edited, revert) verified to parse in v3.0.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_backward_compat_versions.py with v2.0 version manifest fixtures</name>
  <files>tests/integration/test_backward_compat_versions.py, tests/fixtures/v2_versions_manifest.json</files>
  <read_first>
    - bin/wl_versions.py (manifest reading code)
    - lookups/_versions/ (example manifest structure)
  </read_first>
  <action>
Create test that loads pre-rewrite version manifests and verifies v3.0 code handles them:

1. **Create fixture**: tests/fixtures/v2_versions_manifest.json
   - Sample v2.0 manifest format with "versions" key
   - Include 3-5 version entries with timestamps, row counts, author

2. **Test cases**:
   - test_v2_manifest_loads_without_error: Feed v2.0 manifest through wl_versions.read_version_manifest()
   - test_v2_manifest_parses_version_list: Verify versions list is accessible
   - test_v2_manifest_fields_preserved: Verify timestamp, row_count, author fields present
   - test_v2_manifest_iteration_order: Verify versions returned newest-first

3. **Test approach**:
   - Create temporary manifest file from fixture
   - Call wl_versions.read_version_manifest(csv_name)
   - Verify returned tuple (manifest_dict, error_str)
   - Check manifest structure and fields

  </action>
  <verify>
    - tests/integration/test_backward_compat_versions.py exists with >40 lines
    - File contains test_v2_manifest_* test cases (4+)
    - tests/fixtures/v2_versions_manifest.json contains sample manifest
    - All test cases pass: pytest tests/integration/test_backward_compat_versions.py -v
  </verify>
  <done>Backward compatibility versions test created. V2.0 version manifests verified to load and function in v3.0 code.</done>
</task>

<task type="auto">
  <name>Task 3: Create test_backward_compat_approval.py with v2.0 approval queue fixtures</name>
  <files>tests/integration/test_backward_compat_approval.py, tests/fixtures/v2_approval_queue.json</files>
  <read_first>
    - bin/wl_approval.py (queue reading/processing code)
  </read_first>
  <action>
Create test that replays pre-rewrite approval queue entries through v3.0 code:

1. **Create fixture**: tests/fixtures/v2_approval_queue.json
   - Sample v2.0 approval queue entries with action_type, payload, analyst, timestamp
   - Include multiple action types: save_csv, revert_csv, add_rule, delete_rule

2. **Test cases**:
   - test_v2_queue_loads_without_error: Feed v2.0 queue through wl_approval._read_approval_queue()
   - test_v2_queue_entries_parseable: Verify entries are dict with expected keys
   - test_v2_queue_actions_recognized: Verify action_type values recognized by v3.0
   - test_v2_queue_process_approved_action: Feed entry through wl_replay.execute_approved_action()

3. **Test approach**:
   - Create temporary queue file from fixture
   - Call wl_approval._read_approval_queue(csv_name)
   - Verify entries parsed correctly
   - For process test, mock domain functions (read_csv, write_csv, etc.)

  </action>
  <verify>
    - tests/integration/test_backward_compat_approval.py exists with >40 lines
    - File contains test_v2_queue_* test cases (4+)
    - tests/fixtures/v2_approval_queue.json contains sample queue
    - All test cases pass: pytest tests/integration/test_backward_compat_approval.py -v
  </verify>
  <done>Backward compatibility approval queue test created. V2.0 queue entries verified to load and replay correctly in v3.0.</done>
</task>

<task type="auto">
  <name>Task 4: Create test_upgrade_path.sh for end-to-end Docker upgrade test</name>
  <files>scripts/test_upgrade_path.sh</files>
  <read_first>
    - docker-compose.yml (container setup reference)
    - CLAUDE.md (Docker deployment patterns)
  </read_first>
  <action>
Create bash script that performs full upgrade path test in Docker:

1. **Workflow**:
   - Check if v2.0 .spl exists in dist/; if not, build from git checkout of v2.0
   - Stop current container (wl_manager_test)
   - Create fresh container from docker-compose.yml
   - Install v2.0 .spl package
   - Create sample data: CSV file, detection rules, audit events, approval queue entries
   - Capture v2.0 state for comparison
   - Upgrade to v3.0 (copy new .spl, restart Splunk)
   - Verify all data accessible:
     * CSV still loads and displays correctly
     * Audit events still parse in audit.xml
     * Version snapshots still list in revert dropdown
     * Approval queue entries still show in control panel
   - Report PASS/FAIL with data comparison

2. **Script structure**:
   - set -e for fail-on-error
   - Trap to cleanup container
   - Logging of each step
   - Final status report: data preserved (PASS) or lost (FAIL)

3. **Exit code**:
   - 0 if all data accessible after upgrade
   - 1 if any data lost or access fails

  </action>
  <verify>
    - scripts/test_upgrade_path.sh exists with >50 lines
    - Script is executable via test -x
    - Script runs without errors: bash scripts/test_upgrade_path.sh
    - Script reports PASS if data preserved, FAIL if lost
    - Script has proper cleanup trap
  </verify>
  <done>Upgrade path test script created. Full v2.0 to v3.0 upgrade tested end-to-end in Docker with data preservation verification.</done>
</task>

<task type="auto">
  <name>Task 5: Create docs/BACKWARD_COMPAT.md with test matrix and results</name>
  <files>docs/BACKWARD_COMPAT.md</files>
  <read_first>
    - tests/integration/test_backward_compat_*.py (just created)
    - scripts/test_upgrade_path.sh (just created)
  </read_first>
  <action>
Create backward compatibility documentation:

1. **Executive Summary**:
   - v3.0 is backward compatible with v2.0 data
   - All existing audit events, version manifests, approval queues continue to function
   - Safe to upgrade existing installations

2. **Test Matrix**:
   | Data Type | Test Method | Test File | Status | Notes |
   |-----------|------------|-----------|--------|-------|
   | Audit events | Golden event injection | test_backward_compat_audit.py | PASS | Added, removed, edited, revert events |
   | Version manifests | Fixture-based | test_backward_compat_versions.py | PASS | v2.0 manifests load and iterate correctly |
   | Approval queue | Fixture-based replay | test_backward_compat_approval.py | PASS | Queue entries process through v3.0 handlers |
   | Full upgrade path | Docker end-to-end | test_upgrade_path.sh | PASS | Data accessible post-upgrade |

3. **Test Coverage**:
   - Number of audit event types tested: 4 (added, removed, edited, revert)
   - Version manifest versions tested: 3-5
   - Approval queue actions tested: 4+ (save_csv, revert_csv, add_rule, delete_rule)
   - Full upgrade test: v2.0 install + data creation + upgrade to v3.0 + verification

4. **Data Preservation Verification**:
   - CSV data: Readable via get_csv action
   - Audit trail: Queries in audit.xml dashboard still work
   - Version history: Revert dropdown lists all versions
   - Approval queue: Pending requests still show in control panel

5. **Upgrade Steps** (documented for admins):
   - Backup existing app and data
   - Install v3.0 .spl
   - Restart Splunk
   - Verify audit dashboard loads
   - Verify existing CSVs accessible

  </action>
  <verify>
    - docs/BACKWARD_COMPAT.md exists with >50 lines
    - Document contains test matrix table
    - Document contains data types and test methods
    - Document contains test status (PASS/FAIL)
    - Document contains upgrade steps for admins
  </verify>
  <done>Backward compatibility documentation created with test matrix, results, and upgrade instructions for Splunk admins.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run pytest tests/integration/test_backward_compat_*.py -v and verify all pass
2. Run bash scripts/test_upgrade_path.sh and verify PASS status
3. Verify docs/BACKWARD_COMPAT.md has complete test matrix
4. Verify all data types (audit, versions, approval, full path) are tested
</verification>

<success_criteria>
Plan 08-05 is complete when:
- test_backward_compat_audit.py created (>50 lines, 4+ test cases, all pass)
- test_backward_compat_versions.py created (>40 lines, 4+ test cases, all pass)
- test_backward_compat_approval.py created (>40 lines, 4+ test cases, all pass)
- test_upgrade_path.sh created (>50 lines, executable, all pass)
- docs/BACKWARD_COMPAT.md created (>50 lines, test matrix with results)
- All backward compat tests pass: pytest tests/integration/test_backward_compat_*.py -v
- Upgrade path test passes: bash scripts/test_upgrade_path.sh
- All changes committed to git

Requirement PUBL-04 satisfied: Backward compatibility verified and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/08-splunkbase-readiness/08-05-SUMMARY.md` documenting:
- Backward compat test results (audit, versions, approval, upgrade)
- Data preservation verification
- Test coverage (event types, queue actions)
- Upgrade path test status
- Admin upgrade steps
</output>
