#
# Whitelist Manager for Splunk Enterprise Security
# Manage detection rule CSV whitelists with full audit trail.
#

[install]
is_configured = true
build = 405

[launcher]
author       = Security Engineering
description  = Manage detection-rule CSV whitelists with diff-based audit trail
version      = 1.0.0

[ui]
is_visible = true
label      = Whitelist Manager

[package]
id                = wl_manager
check_for_updates = false
