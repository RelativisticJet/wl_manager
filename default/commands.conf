[wlexpiringsoon]
filename        = wl_expiring_soon.py
type            = python
generating      = true
streaming       = false
# AppInspect requires `python.version = python3` (current cert) AND
# `python.required = 3.13` (Splunk 10.2+ future cert). Keep both.
python.version  = python3
python.required = 3.13
