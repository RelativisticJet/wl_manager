# Versions verified by pip-audit 2026-04-29 (round 7 B4).
# pytest >=9.0.3 closes GHSA-6w46-j5rx-g56g (CVE-2025-71176): pre-9.0.3
# pytest leaves the per-user tmpdir world-writable, allowing a local
# attacker on shared dev hosts to symlink-trick a test into reading
# files outside the test's working directory. Local-only, but trivial
# to fix.
pytest==9.0.3
pytest-cov==7.1.0
freezegun==1.5.1
hypothesis==6.90.0
pytest-timeout==2.1.0
# playwright bumped 1.40.0 → 1.60.0 on 2026-05-24 (PR #13). This re-aligns
# the Python pin with Node `playwright-core@^1.59.1` in package.json,
# eliminating cross-language Chromium-binary version skew. Validated
# against the full Python E2E suite under playwright 1.60 (commit
# preceded by conftest selector fix + page_objects rewrite — see
# `tests/e2e/page_objects.py` + `tests/e2e/_shared.py` for context).
playwright==1.60.0
# pytest-playwright removed 2026-05-23 — was declared but never imported.
# Our E2E Python tests use `playwright.sync_api` directly (see
# tests/e2e/conftest.py + page_objects.py); the pytest-fixture wrapper
# was never wired in. Removal unblocks Dependabot PR #12 cleanly:
# bumping an unused dep just adds churn. If we add fixture-style E2E
# later, re-add pytest-playwright at the version we actually integrate
# against.
# radon 6.1.1 was a typo / aspirational pin — never published to PyPI,
# so `pip install -r requirements-dev.txt` failed for any new contributor.
# 6.0.1 is the actual latest and is what `metrics_collector.py` was
# tested against (commit 1202d2c).
radon==6.0.1
